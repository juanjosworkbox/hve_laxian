---
description: "Squad roster schema and cast catalog mapping squad roles to deployed HVE Core agents"
applyTo: '**/.copilot-tracking/squad/**'
---

# Squad Roster Conventions

These conventions define the squad roster: the durable list of roles the Squad Coordinator can dispatch and the HVE Core agent that fills each role. The coordinator reads the roster at the start of every turn to decide who is available, how to invoke them, and which model tier to prefer.

The roster is data, not behavior. It records identities and invocation details. Routing logic lives in `squad-routing.instructions.md`, and persistence rules live in `squad-state.instructions.md`.

## Roster File

The roster lives at `.copilot-tracking/squad/team.md`. The coordinator creates it on first use from the cast catalog below and updates it only through the Squad Scribe.

The file begins with YAML frontmatter and a single H1 title, then a `## Members` table. Each row binds a squad role to a concrete agent.

### Members Schema

The `## Members` table uses these columns:

| Column               | Meaning                                                                                                                                                            |
|----------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Role                 | The squad role name (for example, `lead`, `developer`, `tester`); roles may appear on more than one row when distinguished by `Member Name`                         |
| Member Name          | Optional display name for an individual squad member; required only when two rows share the same `Role` (see *Naming Conventions* below)                            |
| Agent Name (Primary) | The exact `name:` frontmatter value of the deployed HVE Core agent the role resolves to by default                                                                  |
| Alternate Agents     | Optional comma-separated `name:` values the role may resolve to instead, chosen per the row's `Selection Cue`                                                        |
| Selection Cue        | Short condition naming which Alternate applies; `—` when the role has none. Seeded from this catalog so dispatch is a lookup rather than a recollection            |
| Invocation           | How the coordinator dispatches the agent: `runSubagent`/`task` for non-user-facing roles                                                                            |
| Model Tier           | Preferred cost tier: `fast` for read-heavy roles, `default` for reasoning-heavy roles                                                                               |
| Deliverable Root     | The directory this role writes its artifact into, resolved per *Deliverable Roots* below; makes the Artifact Gate a lookup rather than an inference                 |

Model Tier records a preference, not what actually ran, and it never becomes a model name. The concrete model for each dispatch is *resolved* — from an operator declaration, then the dispatched agent's own `model:` frontmatter, then the session model — and captured in the per-dispatch consumption block in `history/<agent>.md` alongside the `model_source` rung that produced it, then aggregated into `consumption.md`, never into `team.md`. Two consequences matter when reading a ledger: an agent that pins `model:` in its frontmatter does not run on the operator's selected model even when its roster tier suggests otherwise, and an agent that pins nothing runs on the operator's model and must be priced at that model's rates rather than its tier's. See *Model Attribution* in `.github/instructions/squad/squad-state.instructions.md`.

The `Agent Name (Primary)` column holds exactly one agent; the role always has a deterministic default. `Alternate Agents` is optional and may be empty for one-to-one roles. `Selection Cue` carries the condition that picks an Alternate; it is seeded here rather than left to the catalog because the catalog is an `applyTo`-scoped instruction file and does not load on every host, while `team.md` is read on every turn. A roster written before this column existed simply has no cue, which resolves to the Primary. The uniqueness key for a row is the (`Role`, `Member Name`) pair, so two rows with the same `Role` are legal when their `Member Name` values differ. When `Member Name` is empty, only one row per `Role` is allowed and the coordinator dispatches that row whenever the role matches. The coordinator resolves the role to a single concrete agent at dispatch time using the *Resolving a Role to an Agent* rules below.

### Members Example

<!-- <example-roster> -->
```markdown
## Members

| Role          | Member Name | Agent Name (Primary)   | Alternate Agents                              | Selection Cue                                                             | Invocation         | Model Tier | Deliverable Root           |
|---------------|-------------|------------------------|-----------------------------------------------|---------------------------------------------------------------------------|--------------------|------------|----------------------------|
| lead          | Alpha       | Squad Lead             |                                               | —                                                                         | runSubagent / task | default    | .copilot-tracking/plans/   |
| developer     | Beta        | Squad Implementor      |                                               | —                                                                         | runSubagent / task | default    | .copilot-tracking/changes/ |
| developer     | Gamma       | Squad Implementor      |                                               | —                                                                         | runSubagent / task | default    | .copilot-tracking/changes/ |
| tester        | Delta       | Squad Reviewer         | Code Review Functional, Code Review Standards | correctness diff → Code Review Functional; conventions diff → Code Review Standards | runSubagent / task | fast       | .copilot-tracking/reviews/ |
| product-owner |             | Functional Planner     | Issue Triage Agent                            | single-issue triage → Issue Triage Agent                                  | runSubagent / task | default    | .copilot-tracking/plans/   |
| scribe        |             | Squad Scribe           |                                               | —                                                                         | runSubagent / task | fast       | (squad state)              |
```
<!-- </example-roster> -->

### Naming Conventions

The `Member Name` column gives each member a human-readable handle that survives across turns. Names are optional. When a row's `Member Name` is empty, the role is dispatched by role alone (the existing single-row-per-role behavior). When two or more rows share the same `Role`, every such row needs a unique `Member Name` so the coordinator can disambiguate at dispatch time via the user-supplied `owner=<Member Name>` hint.

The coordinator picks a name through one of four paths during Init Mode (see the Squad Coordinator's *Init Mode* propose phase):

1. The user supplies a name per member.
2. The coordinator assigns a deterministic alias from the wordlist below.
3. A mix of (1) and (2): the user names selected members; the coordinator fills the rest.
4. The user skips naming: every `Member Name` stays empty and the role-only behavior holds.

#### Deterministic Alias Wordlist

The coordinator picks aliases in order from this list, skipping any name already in use within the seeded roster. The list is intentionally small, ASCII-safe, and stable across runs so two squads seeded with the same profile receive the same default names.

```text
Alpha, Beta, Gamma, Delta, Epsilon, Zeta, Eta, Theta, Iota, Kappa, Lambda, Mu, Nu, Xi, Omicron, Pi, Rho, Sigma, Tau, Upsilon, Phi, Chi, Psi, Omega
```

When the seeded roster needs more than 24 names, the coordinator restarts the list and appends `-2`, `-3`, and so on (`Alpha-2`, `Beta-2`).

#### Naming in a Federation

A federation seeds several rosters in one build, so asking the four-part naming question once per sub-squad would be repetitive. The contract mirrors *Capture in a Federation* in `.github/instructions/squad/squad-notifications.instructions.md`: **ask the policy once, then apply it per sub-squad.**

1. **Ask once, at the federation level.** The Squad Federation Coordinator puts the naming question to the user exactly once per build — during Federation Init Phase 1, Promotion Phase 1, or Expansion Phase 1 — before any sub-squad is seeded. It is the same required question with the same wait-for-the-user gate the single-squad Init applies, and it is never resolved silently to "skip".
2. **What is captured is a policy, not a name list.** The answer is one of the four paths above, plus any per-role names the user supplied. Recording choice 4 (skip) is a decision the user made; never treat an unasked question as choice 4.
3. **Apply the policy to every sub-squad.** The federation coordinator passes the captured policy down with each sub-squad's Init, and the Squad Coordinator running with an inherited naming policy applies it rather than asking again.
4. **Names are scoped to one roster.** Uniqueness is the (`Role`, `Member Name`) pair *within* a single `team.md`, so two sub-squads may both carry an `Alpha`. Under choice 2 the alias wordlist restarts at the top for each sub-squad.
5. **A per-sub-squad override is allowed.** When the user wants different names for one sub-squad, capture them for that sub-squad only and leave the federation policy untouched.
6. **Promotion preserves what exists.** A promoted single squad's `team.md` already carries its `Member Name` column; relocation never renames a member. Ask the naming question only for sub-squads the promotion additionally creates.
7. **Unattended runs never ask.** A Watch Mode bootstrap has no user in the loop: it seeds the event sub-squad under the federation's captured policy, falling back to choice 4 (empty `Member Name`) when the federation has none. It never invents names and never blocks on a question it cannot ask.

## Cast Catalog

The cast catalog is the default casting source and the canonical mapping between squad roles (members) and deployed HVE Core agents, keyed by each agent's exact `name:` frontmatter value. When a project has no `team.md`, the coordinator seeds the roster from this catalog.

The relationship between roles and agents is **many-to-many**. A role names one **Primary** agent — the default the coordinator dispatches — plus optional **Alternate** agents it may resolve to instead when the request matches a **Selection Cue**. A single agent may also fill more than one role (for example, `Codebase Profiler` serves both `researcher` and `security`). See *Relationship Cardinality* below.

Roles that have no stable HVE Core equivalent are marked **thin charter needed**. A thin charter is a small, squad-owned subagent authored under `squad-src/.github/agents/squad/` when the role is actually required; until then the coordinator omits the role or escalates to the user.

Every Primary in this catalog is **dispatchable** (see *Dispatchability* below). Agents that HVE Core ships as user-invocable orchestrators — `PowerPoint Builder`, `Backlog Manager`, `Code Review`, `Documentation`, `RPI Agent`, `Security Reviewer`, `Network ISA-95 Planner`, and the other `disable-model-invocation: true` entry points — are never a Primary and never an Alternate, because `runSubagent` and `task` cannot reach them. Where HVE Core moved a capability from an agent to a skill (research, planning, implementation, review, documentation), the squad dispatches a thin charter that runs that skill.

| Role             | Primary Agent (`name:`)       | Alternate Agents (`name:`)                                                                                          | Selection Cue                                                                                                                                                                                                 |
|------------------|-------------------------------|---------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| lead             | Squad Lead                    | —                                                                                                                   | Author the plan and enumerate the run's deliverables → Squad Lead (squad-owned charter running the `rpi-plan` skill). **Bounded phase revision**: the alternate this role used to reach for revising one numbered phase inside an existing plan was removed outright from hve-core at `14e46010407edaa194bd2bba3d4e100d8707739c`. It shared the exact delegated-worker shape *Worker Agents Are Not Roles* excludes — it validated a required parent plan artifact, an exact assigned phase, and a write boundary limited to that phase before acting — so it was never eligible as a plain role dispatch target in the first place, and no replacement is needed: `Squad Lead` already owns the whole plan, including revising any one of its phases, directly |
| researcher       | Squad Researcher              | Codebase Profiler, Meeting Analyst                                                                                  | Technology-profile scan → Codebase Profiler; meeting-transcript mining → Meeting Analyst; otherwise own the primary research artifact and delegate bounded lanes → Squad Researcher (squad-owned charter running the `rpi-research` skill). `RPI Researcher` is a delegated lane worker and never a roster entry — see *Worker Agents Are Not Roles*. **Knowledge-graph research**: the `graph-research` prompt is a user entry point, not a dispatchable agent, so a request for graph-backed research is escalated to the user to run `/graph-research` rather than dispatched |
| developer        | Squad Implementor             | —                                                                                                                   | Squad-owned charter running the `rpi-implement` skill; writes the change record under `.copilot-tracking/changes/` and stops at every impactful action. **AI application build**: this role also owns building on Microsoft Agent Framework and Semantic Kernel, through the bundled skills registered against it in *Registered External Cast*. No `ai-engineer` role exists, because building the application is what this role already does; the governance side stays with `rai`, `privacy`, and `security` |
| tester           | Squad Reviewer                | Code Review Functional, Code Review Standards, Code Review Security, Code Review Accessibility, Code Review Readiness, Code Review Explainer, Code Review Walkback | Correctness/edge-case diff → Code Review Functional; standards diff → Code Review Standards; security diff → Code Review Security; accessibility diff → Code Review Accessibility; PR deliverable readiness → Code Review Readiness; factual "what does this symbol do" question about reviewed code → Code Review Explainer; bounded investigation behind a review finding → Code Review Walkback; otherwise implementation-versus-plan review → Squad Reviewer. **Reads, never writes tests**: every agent in this row reviews a diff or an implementation against a plan, and none authors or executes a test. Writing and running tests is `qa-engineer`. **Pull-request walkthrough**: the alternate this role used to reach for a PR walkthrough was removed outright from hve-core at `c8c5e94ecd22438e21460bd4b2064f8516f55603`. Its Register 1 walkthrough became `Code Review Orientation`, a mandatory internal stage the `Code Review` orchestrator now runs itself before any findings perspective, not a caller-dispatchable subagent — it consumes a `diff-state.json` only that orchestrator produces, so it fails the *Worker Agents Are Not Roles* contract test and cannot replace the retired alternate here. `Code Review Readiness` already covers PR deliverable readiness, so no gap remains |
| qa-engineer      | QA                            | —                                                                                                                   | **External, opt-in** — see *Registered External Cast*. Test planning, edge-case and hostile-input analysis, writing tests in the project's own framework, running them, and reporting reproducible defects with severity. Distinct from `tester`, which reviews a diff and never writes or runs a test; this role produces and executes the tests. The generated tests land in the project's existing test tree, following the deployed `pester`, `python-tests`, `csharp-tests`, and `rust-tests` instructions rather than a convention this role invents; the test plan and defect report land in its Deliverable Root. The upstream `name:` is the bare string `QA`, which is what the coordinator must dispatch |
| challenger       | Squad Challenger              | —                                                                                                                   | Squad-owned charter running the `rpi-challenger` and `rpi-plan-critique` skills; pressure-tests plans, proposals, and assumptions, and checks a plan against the research it rests on (the capability the retired `Plan Validator` used to provide)                    |
| architect        | System Architecture Reviewer  | ADR Creator                                                                                                         | Decision record → ADR Creator; otherwise design-tradeoff and well-architected review → System Architecture Reviewer                                                                                            |
| security         | Security Planner              | SSSC Planner, Skill Assessor, Supply Chain Skill Assessor, Finding Deep Verifier, Report Generator, Dependency Reviewer, Codebase Profiler | Supply-chain posture → SSSC Planner; single security-skill assessment → Skill Assessor; supply-chain skill assessment → Supply Chain Skill Assessor; verify a finding → Finding Deep Verifier; compile vulnerability report → Report Generator; dependency-change review → Dependency Reviewer; tech profiling → Codebase Profiler; otherwise security planning → Security Planner |
| supply-chain     | SSSC Planner                  | Supply Chain Skill Assessor                                                                                         | Single supply-chain skill assessment against the codebase → Supply Chain Skill Assessor; otherwise Scorecard, SLSA, Sigstore, and SBOM posture assessment and backlog → SSSC Planner. Distinct from `security`: that role threat-models the software, this one assesses how the software is built, signed, and released. Both agents stay listed as `security` alternates, which is legal many-to-one casting. **Distinct from `release-engineer`**: this role assesses and reports posture and never edits the pipeline, while that role authors and changes it. A pinning or token-permission finding raised here becomes an input to `release-engineer`, which is what makes the two complementary rather than competing |
| vuln-manager     | Squad Vulnerability Manager   | —                                                                                                                   | Squad-owned charter running the `vex` skill; triages known CVEs against this product for reachability and exploitability and drafts OpenVEX statements for human merge. Distinct from `security`, which threat-models the design, and from `supply-chain`, which assesses how the software is built. `CVE Analyzer` is `disable-model-invocation: true`, so deep per-CVE reachability analysis is escalated to the user to run `/vex-scan` or `/vex-triage` |
| rai              | RAI Planner                   | RAI Skill Assessor                                                                                                  | Single-framework assessment against the codebase → RAI Skill Assessor; otherwise responsible-AI assessment and planning → RAI Planner                                                                          |
| privacy          | Privacy Planner               | —                                                                                                                   | Single agent — data maps, DPIA thresholds, and privacy controls for a processing activity                                                                                                                     |
| accessibility    | Accessibility Framework Assessor | Accessibility Surface Inventory                                                                                  | Discover the runtime surfaces and interaction states a codebase exposes, before anything can be assessed → Accessibility Surface Inventory; otherwise assess a framework scope (WCAG 2.2, ARIA Authoring Practices, cognitive accessibility, Section 508, EN 301 549) → Accessibility Framework Assessor. Distinct from the `tester` alternate `Code Review Accessibility`, which reviews one diff; this role assesses the product. `Accessibility Planner` and `Accessibility Reviewer` are `disable-model-invocation: true` and cannot be dispatched |
| fact-checker     | Finding Deep Verifier         | —                                                                                                                   | Verification-focused (confirms FAIL/PARTIAL findings); confirm fit before dispatch                                                                                                                            |
| risk-manager     | Squad Risk Manager            | —                                                                                                                   | Squad-owned charter that follows the deployed `risk-register` prompt; qualitative project risk using a probability by impact matrix, written to `docs/risks/`. Distinct from `security` (threat modeling) and `rai` (framework harm assessment), both of which are single categories inside this register. When the prompt file is absent the charter escalates to the user to run `/risk-register` rather than improvising the method |
| designer         | UX UI Designer                | DT Coach, DT Learning Tutor                                                                                          | Facilitated design-thinking session → DT Coach; DT curriculum/learning → DT Learning Tutor; otherwise UX research, JTBD, journey mapping → UX UI Designer                                                     |
| product-owner    | Functional Planner            | Issue Triage Agent                                                                                                  | PRD→work-item hierarchy for Azure DevOps, GitHub, or Jira (select via `platform`) → Functional Planner; single-issue triage → Issue Triage Agent. **Backlog Manager**: HVE Core's own orchestrator (`Backlog Manager`) is user-invocable only and cannot be dispatched, so this role plans the hierarchy directly and stops at the handoff; the live-tracker write belongs to `backlog-executor`, which the coordinator offers to add when the roster does not carry it. **GitLab**: no GitLab agent ships, so this role plans GitLab work the same way and the merge-request and pipeline operations are reached through the `gitlab` skill by the role that owns the change |
| backlog-executor | Squad Backlog Executor        | —                                                                                                                   | Apply a finalized `handoff.md` to a live Azure DevOps, GitHub, or Jira backlog, strictly behind the Impactful-Action Gate; previews read-only and never plans work item content. **Opt-in**: seeded by no profile — see *Opt-In Roles* below                                                                                                    |
| analyst          | PRD Builder                   | BRD Builder, Meeting Analyst                                                                                          | Business requirements → BRD Builder; transcript→requirements → Meeting Analyst; otherwise product requirements → PRD Builder. **Requirements validation**: the general advisory agent this role used to alternate to is no longer shipped by hve-core; a completeness/clarity check on a drafted requirements artifact routes to `intake-validator` instead of this role                                    |
| data-scientist   | Squad Data Scientist          | —                                                                                                                   | Squad-owned charter running the `data-catalog`, `analysis-authoring`, `dataops`, `feasibility`, and `ml-experimentation` skills: data dictionary/profile/catalog → `data-catalog`; EDA notebook, dashboard build, or dashboard test → `analysis-authoring`; data pipeline or dataops test suite → `dataops`; data or ML feasibility study → `feasibility`; ML experimentation setup or production-readiness review → `ml-experimentation`. HVE Core retired its entire dispatchable data-science agent cast (the generative data-spec, notebook, dashboard, and dashboard-test agents) and shipped a data science and engineering coach orchestrator in its place, which sets `disable-model-invocation: true` and cannot be dispatched. **Synthetic data**: the `synth-data-generate` prompt is a user entry point, not a dispatchable agent, so a synthetic-dataset request is escalated to the user to run `/synth-data-generate` rather than dispatched. **Power BI and Fabric**: this role also owns semantic-model review, DAX optimization, report design, and Fabric Lakehouse fundamentals, reached through the opt-in skills registered against it in *Registered External Cast* and named explicitly in the charter. No `bi-analyst` role exists, because a second role would claim the same analytical work; upstream ships no Power BI agent that passes the verification gate |
| prompt-engineer  | Squad Prompt Engineer         | Vally Test Author, HVE Artifact Tester                                                                              | Conformance stimuli → Vally Test Author; artifact conformance run → HVE Artifact Tester; eval dataset → Squad Prompt Engineer running the `evaluation-design` skill; otherwise author, refactor, or analyse a prompt artifact → Squad Prompt Engineer (squad-owned charter running the `prompt-builder`, `prompt-refactor`, and `prompt-analyze` skills). The eval-dataset agent this role used to alternate to is retired with no dispatchable replacement; `evaluation-design` ships only as a skill |
| technical-writer | Squad Technical Writer        | —                                                                                                                   | Squad-owned charter running the `documentation` skill; `Documentation` itself is user-invocable only and cannot be dispatched                                                                                  |
| presenter        | PowerPoint Subagent           | —                                                                                                                   | Owns the deck end-to-end through the `powerpoint` skill pipeline (extract → content YAML → build → validate). `PowerPoint Builder` is the user-invocable orchestrator and cannot be dispatched, so the subagent is the Primary |
| experimenter     | Experiment Designer           | —                                                                                                                   | Single agent — Minimum Viable Experiment design                                                                                                                                                               |
| cost-manager     | Squad Cost Manager            | —                                                                                                                   | Pricing lookups (Azure Retail Prices REST via Squad Researcher), budget envelopes, FinOps-aligned tradeoffs, WAF Cost Optimization checklist (CO:01–CO:14); cost-impact review on plans and architecture        |
| azure-architect  | Squad Azure Architect         | —                                                                                                                   | Azure HLD/LLD authoring with AVM modules and landing-zone patterns; distinct from `architect` (the System Architecture Reviewer reviews tradeoffs while this role authors)                                      |
| scribe           | Squad Scribe                  | —                                                                                                                   | Squad-owned subagent; the ordinary state writer after Cost Preflight and the writer of durable per-agent notes under `/memories/repo/`                                                                         |
| devrel           | —                             | —                                                                                                                   | **No agent and no backing skill.** Unlike the other charter-backed roles, developer relations has no HVE Core capability to wrap, so a charter here would invent a capability rather than expose one. The role stays listed and unselectable until a real skill backs it; escalate this work to the user |
| networking       | —                             | —                                                                                                                   | **No agent and no backing skill.** Network design — topology, segmentation, routing, DNS, load balancing, firewall and peering policy — has no HVE Core capability and nothing in the external cast worth registering. `Network ISA-95 Planner` ships but sets `disable-model-invocation: true` and is scoped to OT and industrial-control segmentation, so it neither dispatches nor covers the general gap. Cloud-specific network *resources* are inside `azure-architect` and `aws-architect` as part of a workload topology; vendor-neutral network architecture is not. Unselectable until a dispatchable resource covers it; escalate this work to the user |
| gcp              | —                             | —                                                                                                                   | **No agent and no backing skill.** Google Cloud has no HVE Core capability and no external coverage: the multi-cloud material in `github/awesome-copilot` is AWS-only, which is why the `aws` pack exists and no `gcp` pack does. This is an absence rather than a decision against GCP. Unselectable until a resource exists to gate; escalate this work to the user |
| identity         | —                             | —                                                                                                                   | **No agent and no backing skill.** Identity architecture — tenant and directory design, conditional access, federation and B2B, entitlement and access reviews, workload-identity strategy — has no HVE Core capability. Upstream coverage is two narrow slices, `entra-agent-user` (provisioning an agent's own identity) and `azure-role-selector` (picking an RBAC role), neither of which founds a role, so neither is registered. Identity *controls* stay with `security` and `privacy`, which already reason about authentication, authorization, and personal data. Unselectable until upstream ships identity architecture rather than two point tools; escalate this work to the user |
| iac-author       | Squad IaC Author              | —                                                                                                                   | Convert the Squad Azure Architect's LLD table into Bicep or Terraform under infra/{track}/{project} with AVM modules; authors IaC but never deploys (deployment is the deployer's role)                          |
| deployer         | Squad Deployer                | —                                                                                                                   | Run Azure deployments (what-if/plan, then gated create/apply) in the consumer's environment, strictly behind the Impactful-Action Gate; defaults to a read-only dry-run                                          |
| release-engineer | GitHub Actions Expert         | —                                                                                                                   | **External, opt-in** — see *Registered External Cast*. Owns the pipeline itself: authoring and hardening GitHub Actions workflows (SHA-pinned actions, least-privilege `permissions:`, OIDC instead of long-lived secrets, concurrency and caching), CI cost and duration, environments and approvals, release trains, and rollout and rollback plans. Distinct from `iac-author`, which authors the infrastructure the pipeline deploys, and from `deployer`, which performs the Azure deployment itself behind the Impactful-Action Gate — this role builds the pipeline, and neither runs it against a subscription nor replaces that gate. **Distinct from `supply-chain`**, which is the nearer boundary and the easier one to blur: that role assesses and reports how the software is built, signed, and released, and this role changes the pipeline that does it. The two share the `github-actions-hardening` skill and use it for different ends — evidence for a posture report there, a fix applied to a workflow file here. It respects the command taxonomy in `.github/instructions/ci-owned-validation.instructions.md` rather than restating it, and the Azure DevOps surface it reaches is pipelines, builds, repos, and artifacts only; work-item operations stay with `product-owner` and `backlog-executor` |
| modernizer       | Squad Modernization Planner   | Squad SQL Migration Advisor                                                                                          | Framework, dependency, deprecated-API, containerization, or general Azure-migration-readiness modernization routes to Squad Modernization Planner. SQL Server-to-Azure path selection, migration-method guidance, downtime/cutover planning, and path-specific prerequisite or readiness plans route to Squad SQL Migration Advisor. |
| asbuilt-author   | Squad As-Built Author         | —                                                                                                                   | Author drop-in as-built artifacts (resource inventory, compliance matrix, operations runbook, DR plan) for already-deployed infrastructure; strictly read-only, never deploys or authors IaC                     |
| azure-diagnose   | Squad Azure Diagnose          | —                                                                                                                   | Read-only triage of deployed Azure resources (Resource Health, Monitor/Log Analytics, configuration) into ranked hypotheses; recommends fixes but never applies them, deferring to the deployer or IaC author. **Also leads the incident lifecycle** — triage, diagnose, mitigate, and the root-cause record — by following the deployed `incident-response` prompt, so no separate `sre` role exists; when that prompt is absent it delivers the diagnosis and escalates the remaining phases to `/incident-response` |
| pp-architect     | Power Platform Expert         | —                                                                                                                   | **External, opt-in** — see *Registered External Cast*. Power Platform solution architecture: component selection across Power Apps, Power Automate, Power Pages, and Copilot Studio; environment and tenant strategy including DLP; Dataverse table, relationship, and security-role modeling; and ALM planning across solutions and pipelines. Advises and authors documents only, and never runs `pac` against a tenant. Distinct from `azure-architect`, which owns Azure resource topology, and from `architect`, which reviews design tradeoffs rather than authoring them. Strengthened by the `power-platform-architect` skill, which turns a transcript or use case into a component architecture |
| pp-connector     | Power Platform MCP Integration Expert | —                                                                                                           | **External, opt-in** — see *Registered External Cast*. Power Platform custom connectors and Copilot Studio agent integration: `apiDefinition.swagger.json`, `apiProperties.json`, and `script.csx` authoring, MCP streamable-HTTP and JSON-RPC schema compliance, OAuth hardening, and connector certification preparation. Authors connector files and never publishes one to a tenant. Strengthened by the `power-platform-mcp-connector-suite` and `mcp-copilot-studio-server-generator` skills |
| m365-agent-architect | Declarative Agents Architect | —                                                                                                       | **External, opt-in** — see *Registered External Cast*. Microsoft 365 Copilot declarative agent design: capability selection across web search, Graph connectors, SharePoint, and code interpreter, instruction authoring, conversation starters, schema-limit validation, and the choice between a JSON manifest and a TypeSpec definition. Authors manifests and design documents only, and never provisions or publishes an agent to a tenant. Distinct from `prompt-engineer`, which authors this repository's own Copilot customization artifacts; this role authors a shipped M365 Copilot product. Strengthened by the `declarative-agents`, `typespec-create-agent`, and `typespec-create-api-plugin` skills |
| m365-agent-integrator | MCP M365 Agent Expert       | —                                                                                                           | **External, opt-in** — see *Registered External Cast*. Wiring a Microsoft 365 Copilot agent to the systems behind it: MCP server tool import and schema mapping, OAuth 2.0 and Entra SSO configuration, Microsoft Graph SDK integration and permission scopes, and the admin-center deployment and governance path. Distinct from `pp-connector`, which builds Power Platform custom connectors and Copilot Studio agents; this role targets M365 Copilot. Its deployment step reaches a live tenant, so it plans and stops at the handoff exactly as `product-owner` does for a tracker write. Strengthened by the `msgraph-sdk`, `mcp-create-declarative-agent`, and `mcp-deploy-manage-agents` skills |
| aws-architect    | aws-principal-architect       | aws-cloud-expert, aws-serverless-architect                                                                          | **External, opt-in** — see *Registered External Cast*. AWS architecture: event-driven and serverless design (Lambda, API Gateway, EventBridge, Step Functions, DynamoDB) → aws-serverless-architect; hands-on service selection, CDK/SAM/CloudFormation authoring, and workload debugging → aws-cloud-expert; otherwise multi-account strategy, Organizations and Control Tower, landing zones, and Well-Architected trade-offs across all six pillars → aws-principal-architect. Advises and authors designs and IaC; never runs a deployment. Distinct from `azure-architect`, which owns the Azure topology — neither role reasons about the other's cloud, and a workload spanning both needs both. All three upstream `name:` values are lowercase slugs rather than title case; dispatch them exactly as written |
| aws-diagnose     | AWS Incident Triage           | —                                                                                                                   | **External, opt-in** — see *Registered External Cast*. Read-only, time-boxed AWS incident investigation: CloudWatch alarms, blast-radius narrowing across account, region, service and resource, metric and log correlation, X-Ray trace sampling, and an evidence-backed root-cause hypothesis with a suggested mitigation. Recommends but never applies. The AWS mirror of `azure-diagnose`, and deliberately narrower: it does not carry the incident lifecycle beyond the hypothesis, because no `incident-response` prompt equivalent exists on this path. Distinct from `observability`, which designs what a system emits before an incident rather than reading it during one |
| performance      | Squad Performance Planner     | —                                                                                                                   | Squad-owned charter running the `performance-slo-planner` skill; SLIs, SLOs, error budgets, load model, test matrix, and reliability backlog. Plans but never executes a load test. Distinct from `cost-manager`, which also reasons about capacity but prices it, and from `azure-diagnose`, which is reactive against a live incident                                            |
| observability    | Squad Observability Planner   | —                                                                                                                   | Squad-owned charter running the `telemetry-foundations` skill; designs spans, metrics, structured logs, their attributes, cardinality budget, and PII handling. Distinct from `performance`, which states what must be measurable, and from the passive `telemetry-overlay` instructions, which keep applying to other roles' artifacts. `copilot-otel-metrics` is `disable-model-invocation: true` and is a different job (Copilot's own telemetry), so it is escalated to the user |
| intake-validator | PRD Quality Reviewer          | BRD Quality Reviewer                                                                                                | Seeded in the `product` and `full` profiles (addable to any roster); dispatched only by the intake gate in `.github/instructions/squad/squad-intake-gate.instructions.md`. PRD input → PRD Quality Reviewer; BRD input → BRD Quality Reviewer. **Product Manager Advisor no longer ships**: this role now defaults to PRD Quality Reviewer for any other requirements-completeness and clarity check, since both quality-reviewer agents already read the artifact for standards conformance regardless of its declared type |

### External Cast

Some work has no HVE Core capability to wrap and no local charter worth authoring, because a public marketplace already covers the domain. The **external cast** is how the squad reaches such a resource without copying it into this package.

A resource arrives by one of two tiers, and the tier decides almost everything else about it:

* **Bundled** — declared as a pinned dependency in this package's `apm.yml`. A consumer who installs hve-squad alone already has it. There is nothing to install and nothing that can be absent.
* **Opt-in** — recorded here with the command the consumer runs deliberately. Whole verticals are opt-in, because shipping every vertical to every consumer taxes the many to serve the few.

The external cast does not weaken the *Never self-fill an absent role* rule; for an opt-in row it gives that rule an actionable remedy. Only an **opt-in** row has three states:

* **Registered and installed** — behaves exactly like any other catalog role.
* **Registered, not installed** — known and installable, but absent at run time. The coordinator escalates with the install command and stops. The role is never seeded into `team.md` and never appears in a seeded roster as available.
* **Not registered** — outside the cast. The coordinator never fetches, installs, or improvises a resource that has no registry row.

A **bundled** row has two states, not three: present, or a broken installation. It can never be *registered but not installed*, because it ships with the package. A bundled artifact that is genuinely missing is an installation fault to be repaired by reinstalling, not an absent role to escalate to the user.

#### Tier Qualification

A resource is **bundled** only when all five of these hold. Each is a pass-or-fail test, not a judgement call:

1. **It strengthens a role that already exists in the cast catalog.** Decidable from the Role column: if filling the row requires inventing a role, the resource is not bundled.
2. **It is a `skill` or `instruction` kind.** An `agent` or a `plugin` changes who gets dispatched, and that belongs to a deliberate opt-in rather than to everyone by default.
3. **It adds no runtime prerequisite beyond its own bundled files.** No MCP server, no CLI to install, no account, no paid subscription, and no network service requiring credentials. A public unauthenticated API the resource calls on its own is fine.
4. **It stays inside a product or platform the role already works in.** Decidable by naming the product: if using the resource means the role picks up a separate product, tenant, portal, or service it does not otherwise touch, then a vertical is arriving rather than a role being strengthened, and a vertical is opt-in however cleanly it passes the other tests.
5. **It passed the verification gate at a resolved commit SHA**, which is the SHA its `apm.yml` entry pins.

Anything else is **opt-in**. Test 3 does most of the work: the FlowStudio Power Automate skills require a paid MCP subscription, so they are opt-in however useful they are. Test 1 does some of the rest, and it is stricter than it looks — a resource that merely overlaps a role the squad already covers well does not qualify. `microsoft-docs` is the instructive near-miss: it is well authored and genuinely useful, but `researcher` can already fetch `learn.microsoft.com` directly, and the skill expects a Microsoft Learn MCP server to beat that. A resource that needs setup to be marginally better than the capability a role already has is not worth a row in either tier.

**Test 4 exists because tests 1 through 3 alone give the wrong answer for a whole class of resource, and wave 4 is the worked contrast.** `microsoft-agent-framework` and `semantic-kernel` are bundled: they are libraries in .NET and Python, which is what `developer` already writes, so the role picks up no new product. The Power BI skills and `fabric-lakehouse` pass tests 1 through 3 exactly as cleanly — they strengthen `data-scientist`, they are skills, and they need nothing to run — yet they are opt-in, because using them means `data-scientist` takes on Power BI and Fabric, which are separate products with their own tenant, licensing, and workspace. Without test 4 the written rule would ship an entire BI vertical to every consumer by default, which is the outcome the two tiers exist to prevent.

#### External Cast Schema

Registered rows use these columns. Every column is required; a row with a blank cell is not registered. `Prerequisites` takes `none` rather than being left empty.

| Column                   | Meaning                                                                                                                                                                    |
|--------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Role                     | The squad role whose need earned the resource its row; must be a role that exists in the cast catalog above. It records demand, not exclusive access — see the note below this table                                 |
| Tier                     | `bundled` or `opt-in`, assigned by *Tier Qualification* above; the tier decides how the resource arrives, how it is re-verified, and whether it can be absent                |
| Resource                 | The upstream resource identifier exactly as the origin publishes it (for example, `azure-pricing`), because both the pin and the install command depend on it being exact    |
| Resource Kind            | `agent`, `skill`, `instruction`, or `plugin`; the kind determines how the resource resolves (see *Resolving an External Role*)                                               |
| Origin                   | The publishing repository and the path to the resource within it, so the row can be re-verified without a search                                                            |
| Install or Entry         | For `bundled`, the pinned `apm.yml` dependency entry including its `#sha`. For `opt-in`, the exact command the consumer runs, copied from the origin's own documentation      |
| License                  | The license the resource is published under, recorded so the attribution posture in `.github/instructions/licensing-posture.instructions.md` can be applied                  |
| Prerequisites            | Any MCP server, CLI, account, or subscription the resource needs before it works, or `none`; a `bundled` row is always `none`, because test 3 of *Tier Qualification* says so |
| Dispatchability Verified | `yes`, `no`, or `n/a` with the reason; `n/a` is correct for a skill or instruction, which is reached rather than dispatched (see *Reaching an External Resource*)               |
| Verified On              | The ISO date the verification gate last passed for this row; this is the column the re-verification policy reads                                                             |

`Resource Kind` carries most of the weight, because kinds resolve differently:

* An **agent** may become a role's Primary or Alternate once installed, provided it passed the gate. An agent is always opt-in.
* A **skill** or **instruction** is never a Primary. It extends the capability of a role that already exists, and it is reached rather than dispatched.
* A **plugin** is a bundle. Register the specific agent or skill inside it that the role needs, and name the plugin only in the install command; do not register a bundle as though it were a single resource.

**The `Role` column records which role's need earned the row, not who is allowed to use the resource.** For a `skill` or `instruction` the distinction matters, because *Reaching an External Resource* below puts an installed skill in the runtime's skills tree where every agent in the session can see it. A second role reaching the same skill is therefore normal, not a duplicate registration, and it does not trigger the gate's collision check — which asks whether two resources claim the same job, not whether two roles read the same reference. Register one row against the role that most directly needs the resource, and add a second row only when a different role needs it for a *different* job that the first row's row would misdescribe. Where two roles share a skill, say where the line between them falls in both of their Selection Cues; `release-engineer` and `supply-chain` sharing `github-actions-hardening` is the worked case.

#### Registered External Cast

| Role         | Tier    | Resource          | Resource Kind | Origin                                                    | Install or Entry                                                                                  | License | Prerequisites | Dispatchability Verified                      | Verified On |
|--------------|---------|-------------------|---------------|-----------------------------------------------------------|---------------------------------------------------------------------------------------------------|---------|---------------|-----------------------------------------------|-------------|
| cost-manager | bundled | `azure-pricing`   | skill         | `github/awesome-copilot`, `skills/azure-pricing/SKILL.md`  | `github/awesome-copilot/skills/azure-pricing#9db369d00f121542e1c99fd9b6cd6f707bade765` in apm.yml | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| cost-manager | opt-in  | `az-cost-optimize` | skill        | `github/awesome-copilot`, `skills/az-cost-optimize/SKILL.md` | `apm install github/awesome-copilot/skills/az-cost-optimize`                                     | MIT     | Azure MCP server and GitHub MCP server, both configured and authenticated, plus deployed Azure resources; its issue-creation step is an impactful action and belongs behind the Impactful-Action Gate | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| privacy      | bundled | `gdpr-compliant`  | skill         | `github/awesome-copilot`, `skills/gdpr-compliant/SKILL.md` | `github/awesome-copilot/skills/gdpr-compliant#9db369d00f121542e1c99fd9b6cd6f707bade765` in apm.yml | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| developer    | bundled | `microsoft-agent-framework` | skill | `github/awesome-copilot`, `skills/microsoft-agent-framework/SKILL.md` | `github/awesome-copilot/skills/microsoft-agent-framework#9db369d00f121542e1c99fd9b6cd6f707bade765` in apm.yml | MIT | none        | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| developer    | bundled | `semantic-kernel` | skill         | `github/awesome-copilot`, `skills/semantic-kernel/SKILL.md` | `github/awesome-copilot/skills/semantic-kernel#9db369d00f121542e1c99fd9b6cd6f707bade765` in apm.yml | MIT    | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| modernizer   | opt-in  | `recommend-migration-path` | skill | `fredgis/sql-migration-advisor`, `skills/recommend-migration-path/SKILL.md` | `copilot plugin marketplace add fredgis/sql-migration-advisor`, then `copilot plugin install sql-migration-advisor@fredgis` | MIT | GitHub Copilot CLI with plugin support; restart after installation and grant read access to the installed plugin directory when prompted | `n/a` — skill, reached rather than dispatched | 2026-08-18 |
| modernizer   | opt-in  | `generate-migration-prerequisite-plan` | skill | `fredgis/sql-migration-advisor`, `skills/generate-migration-prerequisite-plan/SKILL.md` | `copilot plugin marketplace add fredgis/sql-migration-advisor`, then `copilot plugin install sql-migration-advisor@fredgis` | MIT | GitHub Copilot CLI with plugin support; restart after installation and grant read access to the installed plugin directory when prompted | `n/a` — skill, reached rather than dispatched | 2026-08-18 |
| security     | opt-in  | `tm7-threat-model` | skill        | `github/awesome-copilot`, `skills/tm7-threat-model/SKILL.md` | `apm install github/awesome-copilot/skills/tm7-threat-model`                                     | MIT     | Microsoft Threat Modeling Tool v7.3 or later to open the generated `.tm7`; generating the file itself needs nothing | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| supply-chain | opt-in  | `codeql`          | skill         | `github/awesome-copilot`, `skills/codeql/SKILL.md`         | `apm install github/awesome-copilot/skills/codeql`                                                | MIT     | Workflow authoring needs nothing; local analysis needs the CodeQL bundle and SARIF upload needs a `GITHUB_TOKEN` with `security-events: write` | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| supply-chain | opt-in  | `secret-scanning` | skill         | `github/awesome-copilot`, `skills/secret-scanning/SKILL.md` | `apm install github/awesome-copilot/skills/secret-scanning`                                      | MIT     | Config authoring needs nothing; alert triage and push protection need GitHub Secret Protection, and pre-commit scanning needs the `advanced-security@copilot-plugins` plugin | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| supply-chain | opt-in  | `dependabot`      | skill         | `github/awesome-copilot`, `skills/dependabot/SKILL.md`     | `apm install github/awesome-copilot/skills/dependabot`                                            | MIT     | Config authoring needs nothing; live alerts and advisory checks need GitHub Dependabot enabled, and pre-commit scanning needs the GitHub MCP `dependabot` toolset | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| deployer     | opt-in  | `azure-deployment-preflight` | skill | `github/awesome-copilot`, `skills/azure-deployment-preflight/SKILL.md` | `apm install github/awesome-copilot/skills/azure-deployment-preflight`                 | MIT     | Authenticated Azure CLI 2.76 or later, Bicep CLI, and `azd` for `azure.yaml` projects | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| deployer     | opt-in  | `foundry-agent-sync` | skill      | `github/awesome-copilot`, `skills/foundry-agent-sync/SKILL.md` | `apm install github/awesome-copilot/skills/foundry-agent-sync`                                 | MIT     | An Azure AI Foundry project with a deployed model, authenticated Azure CLI, and the Azure AI User role or higher on the project | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| iac-author   | opt-in  | `update-avm-modules-in-bicep` | skill | `github/awesome-copilot`, `skills/update-avm-modules-in-bicep/SKILL.md` | `apm install github/awesome-copilot/skills/update-avm-modules-in-bicep`               | MIT     | Bicep CLI for `bicep lint` and `bicep build`, plus network access to MCR | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| iac-author   | opt-in  | `import-infrastructure-as-code` | skill | `github/awesome-copilot`, `skills/import-infrastructure-as-code/SKILL.md` | `apm install github/awesome-copilot/skills/import-infrastructure-as-code`         | MIT     | Authenticated Azure CLI with access to the target scope, Terraform CLI, and network access to the Terraform Registry | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| architect    | opt-in  | `draw-io-diagram-generator` | skill | `github/awesome-copilot`, `skills/draw-io-diagram-generator/SKILL.md` | `apm install github/awesome-copilot/skills/draw-io-diagram-generator`                   | MIT     | The `hediet.vscode-drawio` VS Code extension to open the result in the editor, or the draw.io web app; generating the file itself needs nothing | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| azure-architect | opt-in | `azure-well-architected-review` | skill | `github/awesome-copilot`, `skills/azure-well-architected-review/SKILL.md` | `apm install github/awesome-copilot/skills/azure-well-architected-review`      | MIT     | Authenticated Azure CLI with live resource access, IaC in the repository, and a configured GitHub MCP server for its issue-creation step | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| pp-architect | opt-in  | `power-platform-expert` | agent   | `github/awesome-copilot`, `agents/power-platform-expert.agent.md` | `apm install github/awesome-copilot/agents/power-platform-expert.agent.md`                  | MIT     | none          | `yes` — sets no `disable-model-invocation` and declares no `tools:` list | 2026-08-05  |
| pp-architect | opt-in  | `power-platform-architect` | skill | `github/awesome-copilot`, `skills/power-platform-architect/SKILL.md` | `apm install github/awesome-copilot/skills/power-platform-architect`                     | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| pp-connector | opt-in  | `power-platform-mcp-integration-expert` | agent | `github/awesome-copilot`, `agents/power-platform-mcp-integration-expert.agent.md` | `apm install github/awesome-copilot/agents/power-platform-mcp-integration-expert.agent.md` | MIT | none | `yes` — sets no `disable-model-invocation` and declares no `tools:` list | 2026-08-05  |
| pp-connector | opt-in  | `power-platform-mcp-connector-suite` | skill | `github/awesome-copilot`, `skills/power-platform-mcp-connector-suite/SKILL.md` | `apm install github/awesome-copilot/skills/power-platform-mcp-connector-suite`  | MIT     | `pac` and `paconn` CLI, and `ConnectorPackageValidator.ps1`, for its validation and certification steps only; generation itself needs none | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| pp-connector | opt-in  | `mcp-copilot-studio-server-generator` | skill | `github/awesome-copilot`, `skills/mcp-copilot-studio-server-generator/SKILL.md` | `apm install github/awesome-copilot/skills/mcp-copilot-studio-server-generator` | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| m365-agent-architect | opt-in | `declarative-agents-architect` | agent | `github/awesome-copilot`, `agents/declarative-agents-architect.agent.md` | `apm install github/awesome-copilot/agents/declarative-agents-architect.agent.md` | MIT | The Microsoft 365 Agents Toolkit VS Code extension and Agents Playground, for its testing and provisioning steps only; design and authoring need none | `yes` — sets no `disable-model-invocation` and declares only `codebase`, which this project has | 2026-08-05  |
| m365-agent-architect | opt-in | `declarative-agents` | skill | `github/awesome-copilot`, `skills/declarative-agents/SKILL.md` | `apm install github/awesome-copilot/skills/declarative-agents`                              | MIT     | The Microsoft 365 Agents Toolkit VS Code extension for its local-debugging workflow only | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| m365-agent-architect | opt-in | `typespec-create-agent` | skill | `github/awesome-copilot`, `skills/typespec-create-agent/SKILL.md` | `apm install github/awesome-copilot/skills/typespec-create-agent`                        | MIT     | none, to generate `main.tsp`; compiling it needs the TypeSpec toolchain the consumer's project already supplies | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| m365-agent-architect | opt-in | `typespec-create-api-plugin` | skill | `github/awesome-copilot`, `skills/typespec-create-api-plugin/SKILL.md` | `apm install github/awesome-copilot/skills/typespec-create-api-plugin`              | MIT     | none, to generate the TypeSpec files; compiling them needs the TypeSpec toolchain the consumer's project already supplies | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| m365-agent-integrator | opt-in | `mcp-m365-agent-expert` | agent | `github/awesome-copilot`, `agents/mcp-m365-agent-expert.agent.md` | `apm install github/awesome-copilot/agents/mcp-m365-agent-expert.agent.md`                | MIT     | An MCP server to integrate against, plus Entra OAuth 2.0 or SSO configuration, for its wiring and testing steps | `yes` — sets no `disable-model-invocation` and declares no `tools:` list | 2026-08-05  |
| m365-agent-integrator | opt-in | `msgraph-sdk`  | skill         | `github/awesome-copilot`, `skills/msgraph-sdk/SKILL.md`    | `apm install github/awesome-copilot/skills/msgraph-sdk`                                           | MIT     | An Entra app registration with consented Graph permissions, for the code it produces to run; the guidance itself needs none | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| m365-agent-integrator | opt-in | `mcp-create-declarative-agent` | skill | `github/awesome-copilot`, `skills/mcp-create-declarative-agent/SKILL.md` | `apm install github/awesome-copilot/skills/mcp-create-declarative-agent`         | MIT     | A reachable MCP server exposing metadata, tools-listing, and tool-execution endpoints, plus the Microsoft 365 Agents Toolkit and OAuth or SSO credentials | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| m365-agent-integrator | opt-in | `mcp-deploy-manage-agents` | skill | `github/awesome-copilot`, `skills/mcp-deploy-manage-agents/SKILL.md` | `apm install github/awesome-copilot/skills/mcp-deploy-manage-agents`                 | MIT     | Microsoft 365 admin-center access with the AI Admin role, and Copilot-licensed users in the target tenant | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| data-scientist | opt-in | `powerbi-modeling` | skill      | `github/awesome-copilot`, `skills/powerbi-modeling/SKILL.md` | `apm install github/awesome-copilot/skills/powerbi-modeling`                                     | MIT     | A running Power BI Modeling MCP server and a live connection to a Desktop or Fabric semantic model; it refuses to advise without inspecting the model first | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| data-scientist | opt-in | `power-bi-model-design-review` | skill | `github/awesome-copilot`, `skills/power-bi-model-design-review/SKILL.md` | `apm install github/awesome-copilot/skills/power-bi-model-design-review`           | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| data-scientist | opt-in | `power-bi-dax-optimization` | skill | `github/awesome-copilot`, `skills/power-bi-dax-optimization/SKILL.md` | `apm install github/awesome-copilot/skills/power-bi-dax-optimization`                 | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| data-scientist | opt-in | `power-bi-performance-troubleshooting` | skill | `github/awesome-copilot`, `skills/power-bi-performance-troubleshooting/SKILL.md` | `apm install github/awesome-copilot/skills/power-bi-performance-troubleshooting` | MIT | none | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| data-scientist | opt-in | `power-bi-report-design-consultation` | skill | `github/awesome-copilot`, `skills/power-bi-report-design-consultation/SKILL.md` | `apm install github/awesome-copilot/skills/power-bi-report-design-consultation` | MIT | none | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| data-scientist | opt-in | `fabric-lakehouse` | skill      | `github/awesome-copilot`, `skills/fabric-lakehouse/SKILL.md` | `apm install github/awesome-copilot/skills/fabric-lakehouse`                                     | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| qa-engineer  | opt-in  | `qa-subagent`     | agent         | `github/awesome-copilot`, `agents/qa-subagent.agent.md`    | `apm install github/awesome-copilot/agents/qa-subagent.agent.md`                                  | MIT     | none          | `yes` — sets no `disable-model-invocation`, and its declared `vscode`, `execute`, `read`, `agent`, `edit`, `search`, `web`, and `todo` tool groups are all exposed here | 2026-08-05  |
| qa-engineer  | opt-in  | `webapp-testing`  | skill         | `github/awesome-copilot`, `skills/webapp-testing/SKILL.md` | `apm install github/awesome-copilot/skills/webapp-testing`                                        | MIT     | Node.js and a running application to drive; it prefers a Playwright MCP server and names a local Playwright install as its own fallback, so it degrades loudly rather than silently | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| release-engineer | opt-in | `github-actions-expert` | agent | `github/awesome-copilot`, `agents/github-actions-expert.agent.md` | `apm install github/awesome-copilot/agents/github-actions-expert.agent.md`             | MIT     | The `github/*` toolset for repository-level operations, reached through the GitHub MCP server or the `gh` CLI per the `github-issue` row of `.github/instructions/squad/squad-mcp-capability.instructions.md`; `actionlint` for local workflow validation. Authoring and hardening a workflow file needs neither | `yes` — sets no `disable-model-invocation`; every other declared tool (`search/codebase`, `edit/editFiles`, `execute/runInTerminal`, `read/readFile`, `search/fileSearch`) is present, and it makes no external lookup a mandatory first step | 2026-08-05  |
| release-engineer | opt-in | `github-actions-hardening` | skill | `github/awesome-copilot`, `skills/github-actions-hardening/SKILL.md` | `apm install github/awesome-copilot/skills/github-actions-hardening`             | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| release-engineer | opt-in | `github-actions-efficiency` | skill | `github/awesome-copilot`, `skills/github-actions-efficiency/SKILL.md` | `apm install github/awesome-copilot/skills/github-actions-efficiency`           | MIT     | Shell access and the `gh` CLI for live measurement and path-gating validation only; static analysis needs none, and the skill states a paste-the-workflow fallback | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| release-engineer | opt-in | `devops-rollout-plan` | skill  | `github/awesome-copilot`, `skills/devops-rollout-plan/SKILL.md` | `apm install github/awesome-copilot/skills/devops-rollout-plan`                         | MIT     | none          | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| release-engineer | opt-in | `azure-devops-cli` | skill     | `github/awesome-copilot`, `skills/azure-devops-cli/SKILL.md` | `apm install github/awesome-copilot/skills/azure-devops-cli`                               | MIT     | Azure CLI 2.81 or later with the `azure-devops` extension, an authenticated `az devops login` PAT session, and an Azure DevOps organization and project | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| aws-architect | opt-in | `aws-principal-architect` | agent  | `github/awesome-copilot`, `agents/aws-principal-architect.agent.md` | `apm install github/awesome-copilot/agents/aws-principal-architect.agent.md`       | MIT     | An authenticated AWS account for the IaC it produces to be deployed; the advice itself needs none | `yes` — sets no `disable-model-invocation` and declares only `execute/*`, `read/*`, `edit/editFiles`, `search`, and `web/*` tools, all present. Its "always fetch AWS documentation first" step runs through `web/fetch`, which this project has, so it cannot degrade silently | 2026-08-05  |
| aws-architect | opt-in | `aws-cloud-expert` | agent         | `github/awesome-copilot`, `agents/aws-cloud-expert.agent.md` | `apm install github/awesome-copilot/agents/aws-cloud-expert.agent.md`                     | MIT     | An authenticated AWS account for the IaC and code it produces to be deployed; the advice itself needs none | `yes` — sets no `disable-model-invocation` and declares only `codebase`, `search`, `edit/editFiles`, `web/fetch`, `runCommands`, `terminalLastCommand`, and `problems`, all present | 2026-08-05  |
| aws-architect | opt-in | `aws-serverless-architect` | agent | `github/awesome-copilot`, `agents/aws-serverless-architect.agent.md` | `apm install github/awesome-copilot/agents/aws-serverless-architect.agent.md`     | MIT     | An authenticated AWS account for the SAM, CDK, or Terraform it produces to be deployed; the advice itself needs none | `yes` — same tool set and same `web/fetch`-backed documentation step as `aws-principal-architect` | 2026-08-05  |
| aws-architect | opt-in | `aws-well-architected-review` | skill | `github/awesome-copilot`, `skills/aws-well-architected-review/SKILL.md` | `apm install github/awesome-copilot/skills/aws-well-architected-review`       | MIT     | An authenticated AWS CLI with read access to the target account, IaC present in the repository (Terraform, CloudFormation, CDK, or SAM), and a configured GitHub MCP server; its issue-creation step is an impactful action and belongs behind the Impactful-Action Gate | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| aws-architect | opt-in | `aws-cost-optimize` | skill        | `github/awesome-copilot`, `skills/aws-cost-optimize/SKILL.md` | `apm install github/awesome-copilot/skills/aws-cost-optimize`                             | MIT     | An authenticated AWS CLI, Cost Explorer enabled on the account, deployed AWS resources, IaC in the repository, and a configured GitHub MCP server; its issue-creation step is an impactful action and belongs behind the Impactful-Action Gate | `n/a` — skill, reached rather than dispatched | 2026-08-05  |
| aws-diagnose | opt-in  | `aws-incident-triage` | agent     | `github/awesome-copilot`, `agents/aws-incident-triage.agent.md` | `apm install github/awesome-copilot/agents/aws-incident-triage.agent.md`                | MIT     | Authenticated read access to CloudWatch alarms, metrics, and Logs Insights, plus X-Ray where traces are sampled, through an AWS MCP server or the AWS CLI; without it the agent has nothing to read | `yes` — sets no `disable-model-invocation` and declares no `tools:` list | 2026-08-05  |

Four bundled rows, deliberately few. `azure-pricing` strengthens `cost-manager`, whose Primary already performs Azure pricing lookups, and it calls a public unauthenticated API; the `Squad Cost Manager` charter names it explicitly in its Step 2, which is the *explicit reach* path described below. `gdpr-compliant` gives `privacy` the code-level engineering rules — data models, retention jobs, logging, pseudonymization — that sit underneath the DPIA and data-map work its Primary already does. `microsoft-agent-framework` and `semantic-kernel` give `developer` the AI application build side that HVE Core covers nowhere, and they are libraries in the languages the squad already writes rather than a separate product. All four were gated against `9db369d00f121542e1c99fd9b6cd6f707bade765`, the same commit their `apm.yml` entries pin, so the recorded outcome stays checkable after upstream moves on.

**The bundled test that decides most of this set is test 3, and the wave 4 harvest is what proved it.** The E-007 list of resources that strengthen existing roles turned out to be almost entirely operational Azure and GitHub skills: `az-cost-optimize` names two authenticated MCP servers in its own prerequisites, `azure-deployment-preflight` and `import-infrastructure-as-code` need an authenticated Azure CLI and a live subscription, and `codeql`, `secret-scanning`, and `dependabot` author their configuration for free but need GitHub security entitlements to operate. Each is genuinely useful and each is opt-in, which is the tier working as designed rather than a shortfall.

The opt-in rows arrived with the vertical packs, with that harvest, and with the two delivery-discipline roles below. A resource earns a row only when a role actually needs it; none is registered to demonstrate a tier. Every path shape — agent file, skill directory, and plugin directory — was confirmed resolvable against `github/awesome-copilot` before any row was written.

**Two verticals are registered against widened existing roles rather than against a pack**, because no upstream agent in either survived the gate. The Power BI and Fabric skills strengthen `data-scientist`, which already owns data profiling and analytical deliverables and now reaches semantic-model review, DAX optimization, report design, and Lakehouse fundamentals as well. The AI application engineering skills strengthen `developer`, which already owns building the application. Inventing a `bi-analyst` or `ai-engineer` role would have put two roles on one job, which the casting rules call worse than a missing role.

**Reaching a widened role is not the same as reaching a pack, and it is worth saying how.** A pack is applied; a widened role is simply on the roster or it is not. `developer` is part of the methodology spine, so the AI application engineering skills are reachable from every profile without anything being added. `data-scientist` is narrower: it is seeded by `full` and `product` only. In any other profile a Power BI, DAX, semantic-model, Fabric, or Lakehouse request matches the `data-scientist` routing row, finds the role absent, and the coordinator offers to add it — the same offer it makes for an opt-in role — rather than declining or answering from a role that does not own the work. That path works, but it is discovered at the moment of need rather than at build time, which is the honest cost of registering a vertical against a role instead of shipping it as a pack.

**Fabric coverage is thin and is recorded as thin.** `fabric-lakehouse` is a Lakehouse primer: OneLake, Delta tables, schemas and shortcuts, SQL analytics endpoint, materialized and Spark views, OneLake security, and V-Order and `OPTIMIZE` tuning. It does not cover Data Factory pipelines, Warehouse architecture, Real-Time Intelligence, deployment pipelines, semantic-model ALM, or capacity administration. A request in those areas has no registered resource behind it and is escalated rather than answered from the primer.

**The two delivery-discipline roles are external for the same reason the verticals are.** QA automation and release engineering are the last uncovered gaps that upstream actually fills: HVE Core ships test-*authoring conventions* (`pester`, `python-tests`, `csharp-tests`, `rust-tests`) with nothing that writes or runs a test, and it ships `workflows.instructions.md`, `ci-owned-validation.instructions.md`, and the `gh-code-scanning` skill with nothing that authors a pipeline. Both therefore rest on a registered opt-in agent rather than on a squad charter, because a charter with no skill beneath it would invent the capability rather than expose one — the same reasoning that keeps `devrel` unselectable.

**One registered row is bundled-eligible and is deliberately left opt-in, and the case for it is argued against a role other than the one that registered it.** `github-actions-hardening` sits under `release-engineer`, because that is the role that most directly needs it: it changes the workflow files the skill audits. The bundled case is argued against `supply-chain`, which reaches the same skill ambiently for posture evidence, and the reason is the tier's own logic rather than an inconsistency. A bundled resource ships to every consumer, so it is only worth bundling if the role it strengthens is one most consumers carry — and `supply-chain` is seeded by `full`, `security`, and `compliance`, while `release-engineer` is seeded by no profile at all. Assessed against `supply-chain` the skill passes all five *Tier Qualification* tests: it strengthens a role that already exists, it is a skill, it needs nothing at run time, GitHub Actions is a platform this repository already lives on, and it gated clean. It stays opt-in only because promoting it means adding an `apm.yml` pin and a curated entry to `scripts/Update-ApmDependencies.ps1`, which is a dependency-manifest change rather than a catalog change. The qualification is recorded here so the promotion is a decision someone makes rather than a discovery someone repeats.

**AWS is a pack and there is no `gcp` anything.** The `aws` pack rests on three verified agents that split cleanly by shape — landing zone and multi-account, hands-on service work, and serverless — plus a fourth for incident triage, so it produces two roles rather than four. The multi-cloud material upstream is AWS-only. The `terraform-aws-*` skills named in the original coverage survey were probed at two paths, `skills/terraform-aws-iam/SKILL.md` and `skills/terraform-aws-vpc/SKILL.md`, and both return 404 at the pinned commit; that establishes those two paths are absent rather than that no such skill exists, so the family was dropped from the candidate set as unresolvable rather than recorded as proven absent. Registering a path that returns 404 would give a consumer an install command that fails. See *Registered Packs* for why AWS is a pack while `azure` remains a profile.

#### Verification Gate

Run these checks in order before a resource is registered. The gate is deliberately cheap so that re-running it is never the reason a row goes stale.

1. **Read the resource's frontmatter at the origin.** Do not register from a listing, a description, or a search result; the frontmatter is the only authority for the checks below.
2. **Check `disable-model-invocation`.** A resource that sets it to `true` can never be a Primary or an Alternate, because `runSubagent` and `task` cannot reach it. This is the same rule *Dispatchability* applies to local agents.
3. **Check the declared `tools:` list against locally available tools.** An agent that requests tools this project does not have degrades silently rather than failing loudly, which is worse than being absent.
4. **Check for a delegated-worker input contract.** A resource that validates a required parent artifact, lane, or cycle before acting is a worker, not a role; *Worker Agents Are Not Roles* applies unchanged.
5. **Check for a collision with a capability the roster can already reach.** Two cases fail this check, not one: a capability HVE Core already deploys, and a resource already registered against the same role for the same job. A duplicate is worse than a gap either way, because the roster stops being a single source of truth about who owns the work.
6. **Check that a role actually needs it.** A resource earns a row only when an existing role's work calls for it. Quality is not the test; demand is. Register nothing to demonstrate a tier, to fill out a domain, or because it may be useful later.
7. **Check the resource's own documentation for a runtime prerequisite.** Frontmatter and the `tools:` list do not surface these: `microsoft-docs` states its Microsoft Learn MCP dependency only in prose, and the FlowStudio skills state a paid subscription only in prose. Record whatever it needs, because this check decides the tier and fills the `Prerequisites` column.
8. **Record the licence and the origin path**, so attribution and re-verification are both possible from the row alone.

Record the outcome in one of exactly two places, never neither:

* **Accepted** — add the row to *Registered External Cast* with `Verified On` set to the date the gate ran.
* **Rejected** — add the resource to the *External Cast Blocklist* with the failing check as the reason, so the next maintainer does not re-evaluate it from scratch.

#### External Cast Blocklist

The blocklist is the rejection ledger. It carries collision rejections, not-needed rejections, and gate failures, and the coordinator never resolves a role to anything listed here.

| Resource                    | Kind   | Reason                                                                                                                                                          | Recorded On |
|-----------------------------|--------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|
| `software-engineering-team` | plugin | Gate step 5, duplicate roles. Ships `se-product-manager-advisor`, `se-security-reviewer`, `se-system-architecture-reviewer`, `se-technical-writer`, and `se-ux-ui-designer`, which duplicate capabilities HVE Core already deploys under its own names. The step number is cited retrospectively: this row predates the gate's numbering and its reason is unchanged | 2026-08-04  |
| `project-planning`          | plugin | Gate step 5, duplicate roles. Overlaps the planning and requirements capabilities HVE Core already deploys through `Squad Lead`, `PRD Builder`, and `BRD Builder`. The step number is cited retrospectively, as for the row above             | 2026-08-04  |
| `azure-logic-apps-expert`   | agent  | Gate step 3. Declares `microsoft.docs.mcp`, `azure_query_learn`, and `azure_get_code_gen_best_practices`, none of which this project has, and its own instructions make documentation search through those tools the *first* step — so it answers from weaker grounding instead of failing, which is the silent degradation the check exists to catch. Same judgment already applied to the `microsoft-docs` skill. Logic Apps is Azure integration rather than Power Platform, so the capability stays with `azure-architect` and `researcher` | 2026-08-05  |
| `power-bi-data-modeling-expert`, `power-bi-dax-expert`, `power-bi-performance-expert`, `power-bi-visualization-expert` | agent | Gate step 3, all four, for the same reason as `azure-logic-apps-expert`. Each declares `microsoft.docs.mcp` in its `tools:` list and each instructs itself to *always* search Microsoft documentation through that server before answering, so without it they degrade silently rather than failing. Blocklisting all four is why the data vertical has no dispatchable Primary and registers its skills against `data-scientist` instead | 2026-08-05  |
| `microsoft-code-reference`  | skill  | Gate step 7. Its own frontmatter says it "works best with Microsoft Learn MCP Server", and its fallback is an `npx @microsoft/learn-cli` install rather than no dependency at all. Same judgment as the `microsoft-docs` near-miss: `researcher` can already fetch `learn.microsoft.com` directly, so a resource that needs setup to be marginally better than a capability the role already has earns no row | 2026-08-05  |
| `threat-model-analyst`      | skill  | Gate step 5. Full STRIDE-A threat modeling of a repository is what `Security Planner` and the deployed `security-planning` skill already do, and a second implementation would give `security` two ways to produce the same artifact. The `.tm7` artifact upstream also offers is genuinely absent here, which is why `tm7-threat-model` is registered and this is not | 2026-08-05  |
| `excalidraw-diagram-generator` | skill | Gate step 5, second case. `architect` already resolves to the registered `draw-io-diagram-generator` for editable committed diagrams, so this collides with a resource registered against the same role for the same job rather than with anything HVE Core deploys. One editable-diagram format per role is enough; a second differs in visual style rather than capability, and `.drawio` is preferred because it versions and edits well in a repository and renders natively on GitHub | 2026-08-05  |
| `agent-governance`          | skill  | Gate step 5. Policy-based tool access control, trust scoring, human approval, and audit trails for agent systems are what `RAI Planner` and the deployed `owasp-agentic` skill already cover, and the squad's own Impactful-Action Gate is the approval mechanism this would duplicate | 2026-08-05  |
| `agent-owasp-compliance`    | skill  | Gate step 5. Checks a codebase against the OWASP Agentic Security Initiative Top 10, which is exactly the deployed `owasp-agentic` skill that `security` and `rai` already reach | 2026-08-05  |
| `agent-governance-reviewer` | agent  | Gate step 5. Reviews agent code for governance gaps, duplicating the same `rai` and `security` path as `agent-governance` while additionally claiming a Primary slot | 2026-08-05  |
| `custom-agent-foundry`      | agent  | Gate step 5. Designs and creates VS Code custom agents, which is precisely what `prompt-engineer` owns through the `prompt-builder` skill. **Correction recorded 2026-08-05:** this row also claimed a gate step 3 failure for declaring `vscode`, `execute`, `agent`, and `github/*`. That claim is withdrawn — the first three are VS Code tool-group names this project does expose, as `qa-subagent` demonstrates by declaring the same three and passing, and `github/*` has a named fallback chain in `.github/instructions/squad/squad-mcp-capability.instructions.md`. The collision is the whole reason, and it is sufficient on its own | 2026-08-05  |
| `agentic-eval`              | skill  | Gate step 5. Rubric-based and LLM-as-judge evaluation of agent output overlaps `evaluation-design` and `Vally Test Author`, both of which `prompt-engineer` already reaches | 2026-08-05  |
| `eval-driven-dev`           | skill  | Gate steps 5 and 7. It duplicates the same evaluation capability, and it additionally requires installing the third-party `pixie-qa` package and running the skill's own `setup.sh`, then refuses to proceed without it. Executing a bundled installer from a community-authored skill is the supply-chain surface the licensing and untrusted-content posture exists to avoid | 2026-08-05  |
| `pinecone-rag`              | skill  | Gate step 6. The squad has no RAG or vector-store role, and the skill is bound to a specific commercial hosted database requiring an account and API key plus a separate LLM provider key. Revisit only if a role is created that owns retrieval infrastructure | 2026-08-05  |
| `foundry-hosted-agent-copilotkit` | skill | Gate step 6. It is explicitly for ongoing work inside an *existing* CopilotKit plus AG-UI application on a paid Azure AI Foundry hosted agent, so it presupposes a stack the squad does not target and cannot scaffold | 2026-08-05  |
| `playwright-tester`         | agent  | Gate step 3. Declares the `playwright` MCP toolset, which this project does not expose and for which the capability map names no fallback, and its own first responsibility forbids generating any code before it has driven the site through that MCP. Without the server it stalls or improvises rather than failing, which is the silent degradation the check exists to catch — the same judgment already applied to the four `power-bi-*-expert` agents. Browser-driven testing is reached instead through the registered `webapp-testing` skill, which names a local Playwright install as its own fallback | 2026-08-05  |
| `scoutqa-test`              | skill  | Gate step 7. It needs a globally installed third-party CLI (`npm i -g @scoutqa/cli`), an authenticated account with a commercial hosted service, and it executes the tests on that vendor's infrastructure against a URL handed to it rather than locally. Same judgment as `pinecone-rag`: a resource bound to one commercial hosted service is not capability the roster can promise a consumer it has | 2026-08-05  |
| `quality-playbook`          | skill  | Gate steps 5 and 8. Its three-pass code review and Council-of-Three spec audit duplicate the deployed `code-review` skill and the perspective subagents `tester` already reaches. Step 8 additionally records that it ships under its own `LICENSE.txt` rather than the repository's MIT, and its own guardrails forbid sub-agent delegation outright — which is how the squad dispatches, so it could not run inside a squad turn without breaking its own contract | 2026-08-05  |
| `devops-expert`             | agent  | Gate step 5. It claims the entire eight-phase DevOps loop, and existing roles already own every phase: `lead` plans, `developer` codes, `tester` and `qa-engineer` test, `release-engineer` builds and releases, `deployer` deploys, `azure-diagnose` operates, and `observability` designs what is monitored. One agent claiming seven roles' phases is exactly the ambiguous-owner failure the casting rules forbid; `release-engineer` takes the pipeline slice it was wanted for | 2026-08-05  |
| `se-gitops-ci-specialist`   | agent  | Gate step 5, twice over. It is a member of the already-blocklisted `software-engineering-team` plugin, and independently of that its CI/CD authoring duplicates `release-engineer` while its deployment-failure triage duplicates `azure-diagnose` | 2026-08-05  |

Add an entry whenever a candidate fails the gate. Two standing rules govern the common cases. **A resource that duplicates a capability the roster can already reach is blocked regardless of its quality**, whether that capability is one HVE Core deploys or one already registered against the same role, because the cost of an ambiguous owner exceeds the benefit of a second implementation. **A resource no role needs is blocked even when it passes every other check**, because a registry that collects what might be useful stops being a statement of what the squad can actually do. Removing a blocklist entry requires the collision, the absent demand, or the gate failure to have actually gone away, recorded with a fresh date.

#### Resolving an External Role

External resolution extends *Resolving a Role to an Agent* rather than replacing it:

1. **Skill and instruction kinds never resolve directly.** The role resolves to its local Primary as usual, and the resource extends what that role can do. See *Reaching an External Resource* below for how it gets used.
2. **Agent kinds resolve through the normal sequence** once installed, and are subject to every casting rule that applies to a local agent, including one Primary per role. Every agent row is opt-in.
3. **A registered opt-in agent that is not installed is an absent role.** The coordinator stops and escalates, and must not perform the work itself or substitute a non-mapped agent. This is the same stop, not a softer variant.
4. **The escalation names the remedy.** State the role, the registered resource, the exact `Install or Entry` command from its row, its `Prerequisites`, and the origin and license, so the user can act immediately and knowingly. Offer the choice the roster already offers for any absent role: install it, name a substitute, or remove the role.
5. **A registered but uninstalled opt-in role is never seeded.** The Scribe writes it into `team.md` only after the resource is present, so a roster never advertises capability the project does not have.
6. **A missing bundled artifact is an installation fault, not an absent role.** It ships with the package, so the remedy is reinstalling hve-squad rather than asking the user to fetch anything. Report it as a broken installation and name the entry from the row.

#### Reaching an External Resource

A skill or instruction is never dispatched, so "how does the role reach it" is a separate question from "how does the role resolve". Once present, an external skill sits in the agent runtime's skills tree where every agent in a session can see it, and it reaches its role by one of two paths. The difference in guarantee is real, and the roster states which path applies rather than implying they are the same:

* **Explicit reach, for a role whose Primary is a squad-owned charter** under `squad-src/.github/agents/squad/`. The charter names the skill in its own instructions, so the reach is deterministic and reviewable. This is the stronger guarantee and the one to prefer whenever the charter is ours to edit. Every role whose Primary is a `Squad`-prefixed charter is in this group, and every such role that currently has a registered resource is wired for it: `cost-manager`, `developer`, `deployer`, `iac-author`, `azure-architect`, and `data-scientist` (the Power BI and Fabric skills, named explicitly in `squad-data-scientist.agent.md`). `performance`, `observability`, `vuln-manager`, and `risk-manager` are in the group too and simply have no registered resource yet. Being in this group is a claim about the charter, so when a resource is registered against one of these roles, wire the charter in the same change rather than leaving the claim ahead of the wiring.
* **Ambient reach, for a role whose Primary is a deployed HVE Core agent.** Those agent files are produced by `apm install` and are never hand-edited, so the skill cannot be named in them. The agent finds the skill through ordinary skill discovery, which is triggered by the skill's own description rather than by a roster rule. This is a weaker, non-deterministic guarantee. `security`, `privacy`, `supply-chain`, and `architect` are in this group.
* **Ambient reach is also the only option for a role whose Primary is an external agent.** A registered opt-in agent installed from a marketplace is no more editable here than a deployed HVE Core agent is, so a skill registered against that role cannot be named in its Primary either. `pp-architect`, `pp-connector`, `m365-agent-architect`, `m365-agent-integrator`, `qa-engineer`, `release-engineer`, `aws-architect`, and `aws-diagnose` are the current cases, and every Power Platform, Microsoft 365, QA, release-engineering, and AWS skill registered against them reaches its role this way. The remedy when that is not good enough is the same one the closing paragraph gives: author a squad charter that owns the work and names the skill, and cast the external agent as an Alternate behind it.

Both paths apply to both tiers, and the tier decides only whether the resource is present to begin with. A **bundled** skill ships with the package, so its reach path is live as soon as the package is installed. An **opt-in** skill reaches its role the same two ways, but only after the consumer runs its install command; before that it is simply absent, and a role that depends on it is weaker rather than broken. When an opt-in skill is worth wiring explicitly, wire the charter for it as though it were bundled — the charter should say what it does when the skill is not present.

Do not paper over the difference between the two paths. When a resource matters enough that ambient reach is not good enough, the honest options are to author a squad charter for that role or to leave the work with the role's existing Primary.

#### Re-Verification Policy

A registered row points at a repository that changes daily, so a single verification at registration time is not enough. What replaces it depends on the tier, because a pin removes the moving target and an install command does not.

**Bundled rows re-verify when their pin moves.** A pinned commit SHA is immutable, so the row cannot decay while the pin holds and a calendar trigger would be ceremony. The risk arrives at the moment someone advances the pin, which is the same risk `Get-HveCoreCastDelta.ps1` already guards for the hve-core dependency: a moved SHA can silently break a roster row. Re-run the gate at the new SHA before the moved pin is committed.

**Opt-in rows re-verify on triggers**, because nothing pins them. Re-run the gate for an affected row when any of these happen:

* A dispatch against a registered external agent fails or returns nothing. Treat this exactly as the roster already treats a broken dispatch: the runtime guard escalates, and the registry row is the thing to correct.
* A squad build or roster seed selects a profile that carries an external role. The build is the natural checkpoint, because it is the moment the row is about to matter.
* `Verified On` is more than 90 days old at the time of a build. This is the backstop for rows that no other trigger has touched.

One trigger applies to **both** tiers: when the HVE Core dependency is upgraded, re-check every registered row for collision. Upstream consolidation can turn a registered external resource into a duplicate of a newly deployed capability, and a duplicate belongs on the blocklist regardless of how it arrives.

The same upgrade trigger reaches past the external cast. A charter binds to upstream capability in three ways — a dispatchable agent name, a named skill, and a deployed prompt path — and all three break the same silent way when upstream renames or removes one. `scripts/Get-HveCoreCastDelta.ps1` compares all three surfaces between two hve-core refs and marks the delta breaking when the squad still references something the new ref no longer exposes, which is what stops an automated pin bump from releasing a squad whose charters resolve to nothing.

Re-verification ends in one of three states: the row passes unchanged and `Verified On` advances; the row still resolves but its entry, prerequisites, or license changed, so those cells are corrected and `Verified On` advances; or the resource fails a check and the row moves to the blocklist with the failing reason. A row is never left with a stale `Verified On` and an unchanged date.

### Dispatchability

A role's Primary must be an agent the coordinator can actually reach. An agent is **dispatchable** through `runSubagent` or `task` only when its frontmatter does **not** set `disable-model-invocation: true`. HVE Core sets that flag on its user-invocable entry points, so those agents are reachable by a person typing their name and by nothing else.

* Never seed a `disable-model-invocation: true` agent as a Primary or an Alternate. A dispatch against one silently returns nothing, and a lighter model that receives nothing tends to fill the gap by doing the work inline — which is exactly the protocol violation *Dispatch Discipline* forbids.
* When the only agent that fits a role is user-invocable, the role is **thin charter needed**: either author a squad-owned charter that runs the same underlying skill, or escalate the step to the user so they invoke that agent themselves.
* The squad-owned charters `Squad Researcher`, `Squad Lead`, `Squad Implementor`, `Squad Reviewer`, `Squad Challenger`, `Squad Technical Writer`, `Squad Prompt Engineer`, `Squad Performance Planner`, `Squad Observability Planner`, and `Squad Vulnerability Manager` exist for exactly this reason. HVE Core moved research, planning, implementation, review, critique, documentation, prompt authoring, performance and SLO planning, telemetry vocabulary, and VEX management from agents to the `rpi-research`, `rpi-plan`, `rpi-implement`, `rpi-review`, `rpi-challenger`, `documentation`, `prompt-builder`, `performance-slo-planner`, `telemetry-foundations`, and `vex` skills; the charters are the dispatchable shells that run them.
* **A prompt is not dispatchable either, and a charter may wrap one.** A `.prompt.md` file is a user entry point that `runSubagent` cannot reach, but it is still deployed into the consumer's `.github/prompts/` as a pinned dependency. A charter may therefore *follow* a deployed prompt: it reads the file at dispatch time and executes its steps, rather than restating the workflow and letting the copy drift. Two rules make this safe. The charter must re-read the file each dispatch instead of working from memory, and it must escalate to the user with the slash command when the file is absent rather than improvising the workflow. `Squad Risk Manager` follows `risk-register.prompt.md`, and `Squad Azure Diagnose` follows `incident-response.prompt.md` for the phases beyond diagnosis. Where upstream promotion of a prompt to a skill would remove the file-path coupling, the charter says so, so the dependency is visible rather than silent.

#### Deferred Reviewer-Class Agents

Four HVE Core agents whose names end in *Reviewer* set `disable-model-invocation: true`, and every one was assessed for a charter rather than chartered by default. All four were **deferred**, and they stay listed here with the reason, following the same visible-deferral precedent as `devrel`.

The common finding is structural: each of the four is an **orchestrator**, not a capability. It profiles the codebase, dispatches assessor subagents, verifies their findings, and asks a report generator to collate them. Every one of those subagents is already dispatchable and already cast. A charter around the orchestrator would therefore duplicate the Squad Coordinator, which performs exactly that loop, rather than expose capability the squad cannot otherwise reach.

| Agent                   | Decision | Reason and the path that already covers it                                                                                                                                                                     |
|-------------------------|----------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Accessibility Reviewer  | Deferred | The `accessibility` role reaches `Accessibility Framework Assessor` and `Accessibility Surface Inventory` directly, and `Report Generator` collates accessibility findings as well as security ones                 |
| RAI Reviewer            | Deferred | The `rai` role reaches `RAI Planner` and `RAI Skill Assessor` directly; the reviewer adds orchestration the coordinator already performs                                                                            |
| SSSC Reviewer           | Deferred | The `supply-chain` role reaches `SSSC Planner` and `Supply Chain Skill Assessor` directly. Its VEX pipeline modes are reached instead through `vuln-manager`, or escalated to the user as `/vex-scan` or `/vex-triage` |
| Privacy Reviewer        | Deferred | The `privacy` role reaches `Privacy Planner` directly. Unlike the other three this one has no dispatchable assessor beneath it, so a charter would have to reimplement the review rather than wrap it — which is the case for authoring a real capability later, not for a thin shell now. Diff-level privacy checks stay with `tester` |

A fifth reviewer-class orchestrator, previously covering the same supply-chain ground as SSSC Reviewer through the same two subagents, was removed outright from HVE Core at `8692fe38cc0415ff8d21aa1b5d8198f008cd4038` with no replacement agent. Its coverage was already fully absorbed by `SSSC Planner` and `Supply Chain Skill Assessor`, both still shipped and dispatchable, so its removal closes no gap and the roster needed no other change here.

Revisit a deferral when the squad has a concrete need the listed path cannot meet. Record the need before authoring the charter, so the roster does not accumulate shells nobody dispatches.

### Worker Agents Are Not Roles

Dispatchability is necessary but not sufficient. Some shipped agents are `user-invocable: false` — so `runSubagent` can reach them — yet still refuse a plain role dispatch, because they are **delegated workers** that validate a strict input contract before doing anything. `RPI Researcher` is the canonical case: it requires a cycle number, a wave type, one bounded lane, an exact lane artifact path, and a distinct **parent primary artifact path**, and it returns `Blocked` without writing when any of those is missing.

A worker like that can never be a role Primary or Alternate, because the coordinator dispatches roles with a role-scoped prompt, not with a delegated-input contract. Seeding one produces a role that blocks on every turn while looking installed and dispatchable.

* Only an agent that accepts a role-scoped prompt belongs in this catalog. When the capability is real but the agent demands a contract, the answer is a charter that owns the parent artifact and constructs that contract — which is what `Squad Researcher` does for `RPI Researcher`.
* A worker's required parent artifact is also the role's Deliverable Root. When the catalog assigns a role a Deliverable Root its Primary is contractually forbidden to write, the row is wrong. Treat that mismatch as the detection test for this class of error.

### Deliverable Roots

Each role writes its artifact into a fixed directory so the Artifact Gate in `.github/instructions/squad/squad-autopilot.instructions.md` is a path lookup rather than a per-run inference. The Scribe records the resolved root in the `Deliverable Root` column of `team.md` when it seeds the roster.

| Role                                              | Deliverable Root                                    |
|---------------------------------------------------|-----------------------------------------------------|
| researcher                                        | `.copilot-tracking/research/<date>/`                |
| lead                                              | `.copilot-tracking/plans/`                          |
| developer, iac-author, pp-connector, m365-agent-integrator | `.copilot-tracking/changes/`               |
| tester, challenger                                | `.copilot-tracking/reviews/`                        |
| qa-engineer                                       | `.copilot-tracking/qa/<date>/`                      |
| prompt-engineer                                   | `.copilot-tracking/prompts/`                        |
| security                                          | `.copilot-tracking/security-plans/`                 |
| rai                                               | `.copilot-tracking/rai-plans/`                      |
| privacy                                           | `.copilot-tracking/privacy-plans/`                  |
| accessibility                                     | `.copilot-tracking/accessibility/`                  |
| supply-chain                                      | `.copilot-tracking/sssc-plans/`                     |
| vuln-manager                                      | `.copilot-tracking/security/vex/`                   |
| performance                                       | `.copilot-tracking/performance-plans/`              |
| observability                                     | `.copilot-tracking/observability-plans/`            |
| release-engineer                                  | `.copilot-tracking/release-plans/`                  |
| risk-manager                                      | `docs/risks/`                                       |
| analyst, product-owner, designer, experimenter, modernizer | `.copilot-tracking/plans/`                 |
| data-scientist                                    | `outputs/`                                          |
| backlog-executor                                  | `.copilot-tracking/workitems/execution/<date>/` (ADO) or `.copilot-tracking/jira-issues/execution/<date>/` (Jira) |
| presenter                                         | `.copilot-tracking/ppt/<date>/<deck-slug>/`         |
| technical-writer                                  | `docs/`                                             |
| architect, azure-architect, aws-architect, pp-architect, m365-agent-architect | `docs/architecture/`     |
| scribe                                            | the squad root itself (state, not a deliverable)    |

**The discovery gate writes into an existing root rather than claiming a new one.** A discovery brief is written by `analyst` and therefore lands in the `analyst` root as `<date>-<topic-id>-brief.md`, which keeps the Artifact Gate a path lookup. The gate's other roles (`designer`, `challenger`, `experimenter`) return findings for the Discovery Verdict and write nothing during a discovery session, so one session produces one artifact rather than four.

**Some agents build their own output path, so the root must be passed as an argument rather than as context.** `presenter` is the clearest case: the `powerpoint` skill pipeline derives `<date>/<deck-slug>/` itself and takes the parent as an explicit output argument. Naming the roster cell in the dispatch prose is not enough — pass it as the pipeline's output path, or the agent composes its own and a hand-edited cell is silently ignored. Verify at the roster cell afterwards: an artifact found at the agent's default path instead is a failed dispatch to report, not a location to accept.

**A role absent from this table returns its findings to the coordinator rather than writing a standalone artifact, and that is the correct shape for it rather than a gap.** `cost-manager`, `deployer`, `fact-checker`, `intake-validator`, `asbuilt-author`, `azure-diagnose`, and `aws-diagnose` each hand a structured result back — an estimate, a deploy result, a verification verdict, an intake verdict, drafted as-built content, or a ranked hypothesis — and the durable record is either the Scribe's history entry or the artifact of the role that publishes it, usually `technical-writer`. The Artifact Gate therefore has nothing to look up for them, which is why the lookup table stays silent rather than naming a directory none of them writes. `qa-engineer` is a hybrid worth stating plainly: its test *code* lands in the project's existing test tree, and only its test plan and defect report land in the root above.

**In a federation, deliverable roots are rebased.** A sub-squad's `squadRoot` is `.copilot-tracking/squad/members/<name>/`, and every root in the table above is written relative to it — a `product` sub-squad's plan lands at `.copilot-tracking/squad/members/product/plans/`, and its deck at `.copilot-tracking/squad/members/product/ppt/<date>/<deck-slug>/`. Only `docs/` and `outputs/` stay at the repository root, because published documentation, architecture, and data-science artifacts are repository-wide outputs rather than per-sub-squad working state — and the notebook and dashboard agents resolve `outputs/` relative to the project root regardless of which squad ran them. A sub-squad that writes a deliverable to the repository-root tracking path has escaped its root; the coordinator treats that as a failed stage and re-dispatches with the rebased path stated explicitly.

**The table holds `squadRoot`-relative roots; `team.md` holds resolved ones.** Read every `.copilot-tracking/...` entry in the table above as `<squadRoot>/...` with the `.copilot-tracking/squad/` prefix elided for the default root, and `docs/` and `outputs/` as the two absolute exceptions that never take a prefix. The Scribe resolves each root against the `squadRoot` it was handed **at seed time** and writes the resolved path into the `Deliverable Root` column, so a sub-squad's roster reads `.copilot-tracking/squad/members/product/plans/` while a plain squad's reads `.copilot-tracking/plans/`. A federation roster seeded with the bare repository-root path is a seeding defect, not a variant: every role on it will write outside its own sub-squad.

**`team.md` is the authority at dispatch time, not this table.** The coordinator hands each dispatched agent the `Deliverable Root` read from the roster row it resolved, and the Artifact Gate looks for the artifact at that same cell. A root the user edited by hand therefore takes effect on the very next dispatch with no reseed and no other change: the table is the seed-time default, and the cell is the running value. Never re-derive a root from the table when the roster carries one, never normalize a user-edited cell back to the default, and never treat a divergence between the two as drift to repair — a consumer pointing a role at their own directory is the column working as intended. A roster refresh preserves edited cells; it reseeds a `Deliverable Root` only for a role whose row it is adding.

## Relationship Cardinality

The mapping deliberately supports three shapes so squad roles can stay human-meaningful while reusing the full HVE Core cast:

* **One-to-one** — a role maps to a single agent with no alternates. Examples: `privacy → Privacy Planner`, `experimenter → Experiment Designer`, `presenter → PowerPoint Subagent`.
* **One-to-many** — a role maps to a Primary plus Alternates, and the coordinator resolves to one agent per the Selection Cue. Examples: `product-owner` resolves across the PRD-to-work-item and single-issue-triage agents by request shape; `tester` resolves across the code-review perspective subagents by review sub-type; `aws-architect` resolves across three external agents by workload shape, which is the first time an *external* role carries Alternates rather than a lone Primary.
* **Many-to-one** — a single agent fills more than one role. Examples: `Codebase Profiler` serves `researcher` and `security`; `Finding Deep Verifier` serves `fact-checker` and `security`; `Meeting Analyst` serves `researcher` and `analyst`.

A shared agent is not a conflict: each role dispatches it with role-scoped context, and the Squad Scribe records which role invoked it under that role's history.

## Resolving a Role to an Agent

The coordinator turns a matched role into exactly one concrete agent at dispatch time:

1. **Default to the Primary agent** named in the role's `team.md` row (seeded from this catalog).
2. **Apply the Selection Cue** — when the request matches the cue in the row's `Selection Cue` cell, dispatch the indicated Alternate instead of the Primary. **A cue must be read, not recalled.** When the row carries no cue, the cue does not match, or this catalog did not load, dispatch the Primary: an alternate chosen because it sounds adjacent to the request swaps the role's methodology without saying so, which is how a research stage runs a transcript miner against a document that is not a transcript.
3. **Verify the agent is installed and dispatchable.** The resolved agent must be present in the project (its APM package deployed into `.github/agents/`) **and** must not set `disable-model-invocation: true`. Check both before dispatching, not after a silent no-op. When either check fails, escalate to the user — treat it the same as a **thin charter needed** role rather than silently substituting.
4. **Apply the external cast when the role is backed by a registered resource.** Follow *Resolving an External Role* in the *External Cast* section: a registered **opt-in** agent that is not installed is an absent role, and the escalation names the exact `Install or Entry` value from its row. A **bundled** resource ships with the package and cannot be absent; a missing one is a broken installation rather than an absent role.
5. **Record any non-primary resolution** through the Squad Scribe, so `history/<agent>.md` reflects the agent that actually ran and the cue that selected it.
6. **Never self-fill an absent role.** When the resolved agent is not installed, not dispatchable, or returns nothing, the coordinator stops and escalates to the user. It must not perform the role's work itself, and must not substitute a non-mapped agent to fill the gap. An absent role blocks the stage until the user installs the agent, names a substitute, or removes the role. A dispatch that returns nothing is an absent role, not an invitation to improvise.

## Casting Rules

* Use the exact `name:` frontmatter value from the deployed agent. Names with spaces are quoted when referenced from prompt or agent frontmatter.
* Prefer a deployed HVE Core agent (Primary or Alternate) over a new charter. Author a thin charter only when a required role has no reasonable **dispatchable** HVE Core fit.
* Prefer registering a third-party resource in the *External Cast* over copying it into this package. Vendoring a third-party file is an exception that needs a stated reason, not the default mechanism.
* Never name a `disable-model-invocation: true` agent as a Primary or an Alternate (see *Dispatchability*).
* Keep exactly one Primary per role so dispatch is always deterministic; list every other valid agent under Alternate Agents with a Selection Cue.
* Treat `fact-checker → Finding Deep Verifier` as a best-fit mapping: the agent verifies findings rather than performing general fact-checking, so confirm it suits the request before dispatch.
* Record any deviation from the catalog (a substituted agent, a non-primary resolution, or a new charter) through the Squad Scribe so the roster stays the single source of truth.
* **Re-validate the catalog against the deployed cast whenever the HVE Core dependency is upgraded.** Agent names move when upstream consolidates agents into skills; a roster row pointing at a name that no longer ships is indistinguishable at run time from a broken dispatch. The coordinator's Step 1 roster-resolution precheck is the runtime guard, but the catalog itself is the thing to correct.

## Squad Profiles

A squad profile is a named, project-tailored subset of the cast catalog. Profiles let a project choose the squad that fits its work instead of always seeding the full cast. The coordinator selects a profile during Init Mode (see the Squad Coordinator agent), and the Squad Scribe stamps the chosen profile's members into `team.md`.

The `scribe` role is always included in every profile — it owns every ordinary squad-state write after Cost Preflight and is never proposed as an optional member. The `intake-validator` role is seeded into the `product` and `full` profiles, where requirement and input artifacts are most central; other profiles can add it on demand, and when the conditional intake gate (`.github/instructions/squad/squad-intake-gate.instructions.md`) would fire in a squad that does not carry the role, the coordinator offers to add it rather than skipping the readiness check.

The opt-in discovery gate (`.github/instructions/squad/squad-discovery-gate.instructions.md`) introduces **no role of its own**: it dispatches `analyst`, `designer`, `challenger`, and `experimenter`, which already own requirements authoring, facilitated ideation, pressure-testing, and hypothesis design respectively. It is therefore **offered only in `product` and `full`**, the two profiles whose rosters can run it — `analyst` writes the brief at every depth and no other profile seeds it. In every other profile the gate is silent rather than escalating, which is the deliberate difference from `intake-validator` above: an unvalidated input that exists is a skipped check worth interrupting for, while an unrequested brainstorm is not. `deep` additionally needs `challenger`, which only `full` seeds, so choosing it in a `product` squad prompts the coordinator to offer to add the role or to run `standard` instead.

Every profile also carries the **methodology spine**: `researcher`, `lead`, `developer`, and `tester` — the four roles that run the HVE Core delivery cycle of Research → Plan → Implement → Review. The spine guarantees that, whatever a project's specialization, the squad can always research a question, plan the work, implement the change, and review the result; each profile adds its specialist roles on top. A user may drop a spine role during Init Mode, but that disables the matching leg of the methodology and the Implementation Gate in `squad-routing.instructions.md` escalates if the removed role is later needed.

Some roles are **artifact-owning**: their `Deliverable Root` cell in `team.md` names a real path rather than `—` or `(squad state)`, so a dispatch is expected to leave a standalone artifact there. Read this from the roster, never from a list of role names — the cell is an operator declaration and a consumer may edit it. `researcher`, `lead`, and `tester` are artifact-owning too, but they own the Research, Plan, and Review stages rather than Implement, so the Implement stage's candidates are every other artifact-owning role, `developer` among them.

When the plan's deliverable list names **two or more Implement-stage candidates**, the work is a set of distinct artifacts rather than a single build, and autopilot fans its Implement stage out across the owning specialists instead of dispatching a single `developer` (see *Deliverable Fan-Out* in `squad-autopilot.instructions.md`). `product` is the canonical case — a requirements document, a refined backlog, a design study, an experiment design, a slide deck, written documentation, and a data notebook, owned by `analyst`, `product-owner`, `designer`, `experimenter`, `presenter`, `technical-writer`, and `data-scientist`. It is not the only one: an `azure` roster carries `azure-architect`, `iac-author`, `asbuilt-author`, `modernizer`, and `architect` alongside `developer`, and an architecture document, an IaC scaffold, and a migration sequence are as distinct from one another as a BRD is from a deck. A plan that names one candidate stays a single build, which leaves `default` and every simple run unchanged.

**Reading eligibility off a fixed list of role names was the defect this replaces.** A live `azure` run carried none of the seven `product` roles, was therefore classified as a single build, produced five specialist artifacts anyway, and improvised a fan-out the pipeline does not define — so none of the per-dispatch recording rules that the fan-out path carries applied to it. It reported ten stages and left no `history/<agent>.md` behind any of them.

The narrower term **deliverable-producing role** — `analyst`, `product-owner`, `designer`, `experimenter`, `presenter`, `technical-writer`, `data-scientist` — survives, and now names one thing only: the roles whose output the *Implementation Gate* treats as the turn's substantive output alongside `developer`. It no longer decides the Implement stage's shape, because a role list cannot see a roster it was not written for.

| Profile         | Members (roles)                                                                                                                                | Choose when the project is…                                              |
|-----------------|------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
| `default`       | researcher, lead, developer, tester, scribe                                                                                                    | General build and delivery work — a balanced team (recommended default)  |
| `full`          | researcher, lead, developer, tester, challenger, architect, azure-architect, iac-author, deployer, asbuilt-author, azure-diagnose, security, supply-chain, vuln-manager, rai, privacy, accessibility, risk-manager, performance, observability, designer, fact-checker, cost-manager, modernizer, prompt-engineer, analyst, product-owner, presenter, technical-writer, experimenter, data-scientist, intake-validator, scribe | You want every deployed capability available except the opt-in roles     |
| `security`      | researcher, lead, developer, tester, security, supply-chain, rai, privacy, fact-checker, scribe                                                | Security-, supply-chain-, threat-, privacy-, or responsible-AI-focused (auth, secrets, ML, LLM, personal data) |
| `design`        | researcher, lead, developer, tester, designer, accessibility, scribe                                                                           | UX/UI or product-design focused, with accessibility alongside            |
| `accessibility` | researcher, lead, developer, tester, accessibility, designer, scribe                                                                           | Accessibility conformance is the goal itself — WCAG 2.2, Section 508, or EN 301 549 assessment and remediation |
| `architecture`  | researcher, lead, developer, tester, architect, azure-architect, cost-manager, scribe                                                          | System design, infrastructure, or architecture-review focused            |
| `azure`         | researcher, lead, developer, tester, azure-architect, iac-author, deployer, asbuilt-author, azure-diagnose, architect, cost-manager, security, modernizer, scribe                                    | Azure-focused build with budget and security oversight (Bicep, landing-zone, FinOps signals) |
| `modernization` | researcher, lead, developer, tester, modernizer, architect, azure-architect, iac-author, cost-manager, asbuilt-author, scribe                  | Legacy uplift — framework or dependency upgrades, re-platforming, SQL or cloud migration |
| `compliance`    | researcher, lead, developer, tester, security, supply-chain, vuln-manager, privacy, rai, accessibility, risk-manager, scribe                   | Conformance evidence is the goal — an audit, an attestation, or a customer questionnaire that needs privacy, accessibility, responsible-AI, supply-chain, and vulnerability answers together |
| `operations`    | researcher, lead, developer, tester, azure-diagnose, performance, observability, asbuilt-author, iac-author, deployer, scribe                  | Running and keeping a deployed system healthy — incident response, reliability targets, instrumentation design, and as-built documentation |
| `product`       | researcher, lead, developer, tester, analyst, designer, product-owner, presenter, technical-writer, experimenter, data-scientist, intake-validator, scribe                       | Business discovery and delivery — requirements, design thinking, roadmap, and stakeholder deliverables (often non-technical) |

### Profile Selection

The coordinator chooses a profile in this order of precedence:

1. **Explicit choice** — the user names a profile (for example, `profile=security`) or confirms one during Init Mode.
2. **Project discovery** — the coordinator infers a profile from repository signals when the user does not name one:
   * Source files, tests, and package manifests with no specialized signal → `default`.
   * Authentication, secrets, threat modeling, ML/LLM, or data-handling signals → `security`.
   * Frontend frameworks (React, Vue, Svelte, Angular) or CSS with no explicit conformance signal → `design`.
   * WCAG, ARIA, Section 508, EN 301 549, VPAT, or an existing `.copilot-tracking/accessibility/` tree → `accessibility`.
   * Bicep templates plus budget, pricing, FinOps, or `cost-manager` signals (or `.bicep` files alongside an Azure landing-zone reference) → `azure`.
   * End-of-life runtimes or frameworks, pinned-back dependency manifests, `.NET Framework` or legacy Java projects, or migration and re-platforming documents → `modernization`.
   * Audit, attestation, conformance-questionnaire, or regulatory documents, or an existing `docs/risks/` tree alongside privacy or accessibility tracking artifacts → `compliance`.
   * Runbooks, incident or postmortem records, SLO or error-budget documents, dashboards-as-code, or an OpenTelemetry collector configuration → `operations`.
   * Infrastructure-as-code (Bicep, Terraform without Azure-specific cost signals), system-design docs, or component diagrams → `architecture`.
   * Requirements documents (BRD/PRD), product or roadmap docs, discovery/design-thinking artifacts, or a repository with little or no source code where the work is business discovery and delivery → `product`.
   * Mixed or unclear signals → propose `default` and offer `full`.
3. **Fallback** — when discovery is inconclusive and the user gives no hint, propose `default` as the recommended profile.

A profile only ever lists roles that exist in the cast catalog. A role with no dispatchable Primary — currently `devrel`, `networking`, `gcp`, and `identity`, none of which has a backing skill either — is never part of a profile until a resource exists that a charter could wrap.

**Packs are selected alongside the profile, never instead of it** (see *Squad Packs* below). An explicit `pack=` hint wins; otherwise the coordinator proposes a pack from either of two signals. The first is the repository: for `power-platform`, a `.cdsproj` or `solution.xml`, a `pac` CLI configuration, a canvas app `.msapp`, or a Dataverse solution folder; for `m365-copilot`, a `declarativeAgent.json` or `manifest.json` Copilot agent manifest, an `m365agents.yml` or `teamsapp.yml`, an `appPackage/` folder, or a `main.tsp` importing `@microsoft/typespec-m365-copilot`; for `aws`, a `template.yaml` with `AWS::Serverless` transforms or any CloudFormation template, a `cdk.json`, a `samconfig.toml`, a `serverless.yml`, an `.aws/` configuration directory, or Terraform declaring the `hashicorp/aws` provider. The second is the **request itself** — a user who asks to build a Power Automate approvals flow into an empty repository has told the coordinator the domain before a single file exists, and waiting for disk evidence that will not arrive until after the work starts would be perverse. Either signal is enough to propose; neither is enough to apply. The coordinator presents the profile and any proposed packs together for a single confirmation, so the user never answers two questions where one will do. A proposed pack whose external resources are not installed is presented with its install commands rather than seeded.

### Opt-In Roles

An **opt-in role** is a catalog role that no profile seeds, not even `full`. It is selectable in a custom roster; it simply never arrives by default. A role becomes opt-in for one of two reasons, and the reason changes what the coordinator says when the role is missing.

**Reason one: its work writes somewhere the user may not expect a squad to reach.** `backlog-executor` is the only role in this class today. It writes into a live Azure DevOps or Jira backlog, where a create is announced to a whole team by notifications, subscriptions, and webhooks the moment it lands. Arriving in a seeded roster would make that reach a surprise, so the role is added deliberately or not at all. The role is fully dispatchable the whole time; only its seeding is withheld.

**Reason two: its Primary is a registered opt-in external resource that is absent until the consumer installs it.** `qa-engineer`, `release-engineer`, `aws-architect`, `aws-diagnose`, and every pack role (`pp-architect`, `pp-connector`, `m365-agent-architect`, `m365-agent-integrator`) are in this class. These roles are opt-in by construction rather than by policy: *Resolving an External Role* already forbids seeding a registered-but-uninstalled role, so no profile could carry them honestly even if it wanted to. This is also why the rule that `full` contains every non-opt-in role stays true while `full` carries none of them.

An opt-in role is offered on demand rather than hidden. When a request matches an opt-in role's routing pattern in a squad that does not carry it, the coordinator escalates and **proposes adding the role** — naming what it would be able to write and to which project, and, for a reason-two role, naming the exact `Install or Entry` command from its registry row together with its `Prerequisites` — instead of silently declining or falling back to a role that cannot do the work. This is the same offer the coordinator makes for `intake-validator` when the intake gate fires in a squad without it. On acceptance the Squad Scribe appends the role to `team.md` and its routing rows to `routing.md`, and the turn continues; on decline the coordinator reports what it cannot do rather than substituting.

Adding an opt-in role is a roster change like any other: it persists for the project until the user removes it, and the Scribe records the addition so the roster stays the single source of truth. For a reason-two role the resource must be present first; a roster never advertises capability the project has not installed.

### Squad Packs

A **pack** is a named, additive set of catalog roles layered onto a profile. A profile answers *what kind of work is this*; a pack answers *what is it built on*. Technology verticals arrive as packs, because a profile is single-choice while a domain is not mutually exclusive with a concern — a Power Platform project can need `compliance` evidence exactly as easily as a general build can.

A pack is not a replacement for a custom roster, it is the maintained version of one. A custom roster is assembled by hand, dies with the project, and is discoverable by nobody; a pack is written down once, named, and reusable across every profile.

#### Profile or Pack

Three tests decide which shape a new member set takes, and for everything currently in the catalog all three agree:

| Test                                                                    | Profile                                          | Pack                                      | Federation                                        |
|-------------------------------------------------------------------------|--------------------------------------------------|-------------------------------------------|---------------------------------------------------|
| **Self-sufficiency** — does the set carry the methodology spine and deliver alone? | Yes                                    | No, it is specialists only                | Yes, once per sub-squad — the spine is duplicated |
| **Exclusivity** — is choosing two at once incoherent?                    | Yes, one dominant team shape wins                | No, two can sensibly apply together       | No, but each runs its own turns                   |
| **Question answered**                                                    | "What kind of work is this?" — a mode or concern | "What is it built on?" — a technology domain | "Who owns this stream of work?" — a separate team |

`azure` and `modernization` sit closest to the line and stay profiles by decision rather than by oversight: this repository's work is intrinsically Azure- and modernization-shaped, so each behaves as the dominant team shape rather than as an addition to one. The tests govern **new** capability from here on.

#### Pack or Federation

A pack and a federation both add capability a single profile does not carry, so the two get confused. They solve different problems, and one rule separates them:

> **One piece of work that needs extra expertise is a profile plus a pack.** Those roles must share a plan, a council, and a review.
> **Two streams of work with separate deliverables and owners is a federation.** Those teams need coordination, not the same room.

The reason is mechanical rather than stylistic. A pack composes *within* one roster: `pp-architect` and `privacy` are dispatched in the same turn against the same request, and both findings land in one Council Verdict, one plan, and one review. A federation coordinates *between* rosters: each `members/<name>/` is a complete squad with its own `team.md`, `decisions.md`, `history/`, and consumption ledger, and meta-routing decides which sub-squad owns a turn. Split one piece of work across two sub-squads and its two halves are decided in two councils that never met.

Federation also does not remove the need for a pack — it multiplies it. A sub-squad is seeded from a profile, and a technology vertical reaches that sub-squad the same way it reaches any roster: by applying the pack to it. There is no path from federation to `pp-architect` that does not go through `power-platform`.

A compliance-plus-Power-Platform project is therefore `profile=compliance pack=power-platform`, not two sub-squads, because compliance is a lens on the work rather than a stream of it. Two sub-squads earn their keep when there really are two products — a Power Platform app and an Azure data platform, different owners, shipping separately — and the first of those still carries the pack. See `.github/instructions/squad/squad-federation.instructions.md` for the federation mechanics.

#### Composition Rules

* **A pack selects catalog roles; it never defines them.** Every role a pack names already exists in the *Cast Catalog* with exactly one Primary, so two packs naming the same role produce a deduplicated union rather than a conflict, and the role is seeded once.
* **A pack never introduces a role that duplicates work an existing role already owns.** Check the catalog first and widen the existing role instead — the same rule that gave the incident lifecycle to `azure-diagnose` rather than to a second `sre` role. Two roles claiming one phase is worse than a missing role, and that stays true across packs.
* **Exactly one profile, zero or more packs.** A pack is never chosen *instead of* a profile, because a pack carries no methodology spine and cannot research, plan, implement, and review on its own.
* **A pack's roles obey every rule a profile's roles obey.** In particular, a role whose registered external resource is not installed is offered with its `Install or Entry` command rather than seeded, per *Building a Custom Roster* below.
* **The Scribe records the roster's provenance when it seeds.** A roster is a profile plus zero or more packs, or `custom`, and that is written into the Init decision in `decisions.md`; in a federation it is also the `Profile` column of the `federation.md` registry, in `azure +power-platform` form. A pack applied later is recorded the same way, as a decision, exactly like an added opt-in role.
* **A pack can be applied to a roster that already exists**, and it is offered rather than assumed. When a request matches the routing pattern of a role that belongs to a registered pack the roster does not carry — or names the pack's domain plainly, which is the same signal arriving in words rather than keywords — the coordinator escalates and proposes applying the pack, naming its roles, what they would add, and the install command for any registered resource that is not yet present. This is the same offer the coordinator already makes for `intake-validator` and for an opt-in role, one level up: a group of roles instead of one. On acceptance the Scribe appends the pack's roles to `team.md`, their rows to `routing.md`, and a decision recording the applied pack; on decline the coordinator reports what it cannot do rather than substituting a role that cannot do the work.

#### Removing a Pack

A pack is removable, and removing one is a roster change like any other rather than a special operation. A user who no longer works on a vertical should not carry its roles, its routing rows, or its context.

Removal is safe for the record because of how squad state is already split. `decisions.md` and `history/<agent>.md` are **append-only**: removing a pack appends a new decision and edits nothing, and every dispatch a removed role ever ran stays in its history file. `team.md` and `routing.md` use replace semantics, so their rows come out cleanly. Nothing that happened is unwritten.

Four rules keep a removal honest:

* **Remove only what the pack still owns.** Drop a role only when the pack contributed it *and* nothing else on the roster does — not the base profile, not another applied pack, and not the user's own hand-added row. A role two packs share survives the removal of one of them, which is the same deduplication that applied when the packs were added, run in reverse.
* **Keep the spend visible.** `consumption.md` mirrors roster order and recomputes the run total, so silently dropping a removed member's row would understate what the project actually spent. Keep the row and mark it removed; the run total stays truthful and the per-dispatch blocks in `history/<agent>.md` remain the durable evidence behind it.
* **Leave the work where it is.** Deliverables a removed role already wrote stay on disk. Removing a role withdraws it from future dispatch; it does not retract what it produced.
* **Removal is not an uninstall.** The pack's registered external resources stay installed and stay registered in the *External Cast*. Re-applying the pack later reseeds the roles with no reinstall, and a consumer who wants the disk space back uninstalls the resource deliberately, as a separate act.

Removing a single role follows the same four rules. A pack is only the convenient way to name several at once.

#### Registered Packs

| Pack             | Adds (roles)               | Choose when the project is…                                                                          | Arrival                                                                                                       |
|------------------|----------------------------|-------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------|
| `power-platform` | pp-architect, pp-connector | Built on Power Platform — Power Apps, Power Automate, Dataverse, Power Pages, or Copilot Studio        | Opt-in. Both roles rest on registered external agents; the install commands are in *Registered External Cast* |
| `m365-copilot`   | m365-agent-architect, m365-agent-integrator | Built on Microsoft 365 Copilot — declarative agents, TypeSpec agent definitions, API plugins, MCP-backed agents, or Microsoft Graph integration | Opt-in. Both roles rest on registered external agents; the install commands are in *Registered External Cast* |
| `aws`            | aws-architect, aws-diagnose | Built on AWS — Lambda and serverless, ECS or EKS, CDK, SAM, CloudFormation, Organizations and landing zones, or a live AWS workload to triage | Opt-in. Both roles rest on registered external agents; the install commands are in *Registered External Cast* |

**AWS is a pack while `azure` is a profile, and that asymmetry is deliberate.** `azure` predates the pack mechanism and stays a profile by the decision recorded in *Profile or Pack*: this repository's work is intrinsically Azure-shaped, so an Azure squad behaves as the dominant team shape rather than as an addition to one. AWS is new capability, so the three tests govern it, and all three say pack — it carries no methodology spine, it is not incoherent alongside any profile, and it answers "what is it built on". The practical consequence is the one the tests intend: a multi-cloud project takes `profile=azure` plus `pack=aws` and gets both, where two competing profiles would have forced a false choice. An AWS-only project takes whichever profile matches its *work* — usually `architecture` or `default` — plus the pack. Carrying the `aws` pack says the squad can reason about AWS; it does not imply general multi-cloud capability, and it says nothing at all about Google Cloud, which has no coverage anywhere (see the `gcp` row in the *Cast Catalog*).

**Worked pairings.** The profile is chosen from the shape of the work and the pack from the technology, so the same pack appears against several profiles. These are illustrations rather than an allowlist — any pack composes with any profile:

| Situation                                                       | Profile        | Pack                          |
|-----------------------------------------------------------------|----------------|-------------------------------|
| Greenfield Power Platform app                                    | `default`      | `power-platform`              |
| Power Platform app that has to produce audit evidence            | `compliance`   | `power-platform`              |
| Designing an AWS workload before anything is built               | `architecture` | `aws`                         |
| One estate spanning Azure and AWS                                | `azure`        | `aws`                         |
| On call for a live AWS workload                                  | `operations`   | `aws`                         |
| A Microsoft 365 Copilot agent that handles personal data         | `security`     | `m365-copilot`                |
| A Power Platform solution fronted by an M365 Copilot agent       | `default`      | `power-platform`, `m365-copilot` |

The `operations` plus `aws` row is the case a profile could never have served: `operations` already carries `azure-diagnose`, and the pack adds `aws-diagnose` beside it, so one squad triages both clouds. The last row is the case a profile could not have served either, because two verticals apply at once and a profile is single-choice.

Power BI is deliberately absent from the `power-platform` pack. Its upstream agents serve analytics modeling and report performance rather than Power Platform application delivery, so they belong with the data and analytics vertical, which is registered against `data-scientist` rather than as a pack of its own.

**Two adjacent Microsoft verticals were assessed and deliberately produced no pack.** Data and Power BI, and AI application engineering, both reached the verification gate with no agent surviving it: the four `power-bi-*-expert` agents fail gate step 3, and every AI-engineering candidate duplicates capability HVE Core already deploys. A pack selects roles, and a role needs a dispatchable Primary, so inventing one would have meant authoring a squad charter purely to make a pack exist — and in the AI-engineering case that charter would have claimed work `developer` already owns. Both verticals are therefore registered as skills against widened existing roles, and their surviving resources are listed in *Registered External Cast*. Revisit the pack shape only when an upstream agent in either domain passes the gate.

There is no Power Platform ALM or solution-deployment role. No verified resource can perform a solution import, and a role with no resource behind it is aspirational rather than dispatchable. ALM *planning* — environment strategy, solution segmentation, managed versus unmanaged, pipeline design — sits with `pp-architect`, which advises and never executes. Should an ALM writing role ever be added, it inherits both existing write postures rather than a default: opt-in like `backlog-executor`, because a solution import is announced to everyone in the target environment the moment it lands, and preview-first like `deployer`, because the import itself is not cleanly reversible.

### Building a Custom Roster

When no named profile fits — or when one is close but not exact — the coordinator helps the user assemble a custom roster rather than inventing one. The coordinator presents the role menu below — each row is a role the squad can dispatch, the plain-language work it contributes, and the deployed agent that fills it by default — and the user picks any subset. The user may start from a profile's roles and add or remove from there; when they do, the roster is recorded as a custom roster derived from that profile, because any change to a profile's exact member set makes it custom.

Three rules bound a custom roster so it never references work the squad cannot actually do:

* **`scribe` is always included** — it owns ordinary squad-state writes after Cost Preflight and is never offered as optional.
* **The methodology spine (`researcher`, `lead`, `developer`, `tester`) is recommended** so the Research → Plan → Implement → Review cycle stays intact. The user may drop a spine role, but that disables the matching leg and the Implementation Gate in `squad-routing.instructions.md` escalates if it is later needed.
* **Only catalog roles with a dispatchable Primary are selectable.** The coordinator never invents a role or an agent outside the cast catalog. A role whose mapped agent is not installed or is not dispatchable, or a role with no backing capability at all — `devrel`, `networking`, `gcp`, and `identity` — is flagged and left out rather than seeded. A role backed by a registered **opt-in** *External Cast* resource that is not installed is offered with its install command rather than seeded, and becomes selectable once the resource is present. A **bundled** resource never creates this situation, because it ships with the package.

When the roles a user is reaching for are already a registered **pack**, offer the pack by name instead of walking them through the menu row by row. Applying a pack keeps the roster's provenance recorded as a profile plus that pack, where a hand-picked equivalent would be recorded only as `custom`.

The menu mirrors the Cast Catalog above; each item names the role, the deployed agent that fills it by default (in parentheses), and the user-facing gloss.

* **researcher** (Squad Researcher) — Investigates the codebase, the web, and connected tools to gather the context the squad needs.
* **lead** (Squad Lead) — Plans the work: breaks a request into phases, sequences them, and names the deliverables and their owners.
* **developer** (Squad Implementor) — Implements the change: writes and edits code to carry out the plan.
* **tester** (Squad Reviewer) — Reviews changes for quality, correctness, and standards before they ship.
* **qa-engineer** (QA) — Writes and runs the tests: plans what to cover, hunts edge cases and hostile inputs, and reports defects you can reproduce. Where `tester` reads a change, this role tests it. Opt-in: rests on a registered external agent you install.
* **challenger** (Squad Challenger) — Plays devil's advocate: pressure-tests plans and assumptions, and checks that a plan is backed by the research it claims to rest on.
* **architect** (System Architecture Reviewer) — Reviews system-design tradeoffs and well-architected alignment; can produce ADRs.
* **security** (Security Planner) — Plans security: threat-models the work, identifies risks, and maps controls.
* **supply-chain** (SSSC Planner) — Assesses how the software is built, signed, and released: OpenSSF Scorecard, SLSA, Sigstore, and SBOM posture, and the backlog to improve it.
* **vuln-manager** (Squad Vulnerability Manager) — Works out whether a published vulnerability actually reaches your product, and drafts the VEX statement that records the answer for a human to merge.
* **rai** (RAI Planner) — Assesses responsible-AI concerns such as fairness, harm, and transparency for AI/ML work.
* **privacy** (Privacy Planner) — Maps personal-data flows, tests DPIA thresholds, and plans privacy controls.
* **accessibility** (Accessibility Framework Assessor) — Assesses the product against WCAG 2.2, ARIA, Section 508, and EN 301 549, and discovers the runtime surfaces and interaction states that need assessing.
* **fact-checker** (Finding Deep Verifier) — Independently verifies findings and claims before the squad trusts them.
* **risk-manager** (Squad Risk Manager) — Keeps the project risk register: what could go wrong, how likely and how bad, who owns it, and what the mitigation is.
* **performance** (Squad Performance Planner) — Turns "it should be fast" into measurable objectives, error budgets, and a load-test plan; plans the tests but never runs them.
* **observability** (Squad Observability Planner) — Designs what the system emits and what it is called: traces, metrics, logs, and how personal data is kept out of them.
* **designer** (UX UI Designer) — Researches users and designs the experience: journey maps, jobs-to-be-done, and accessibility.
* **product-owner** (Functional Planner) — Turns a PRD into a validated work-item hierarchy for Azure DevOps, GitHub, or Jira. Plans the work items; `backlog-executor` writes them.
* **backlog-executor** (Squad Backlog Executor) — Creates the planned work items in your live Azure DevOps, GitHub, or Jira project, after showing you exactly what it will write and waiting for your approval. Opt-in: no profile includes it, and the squad offers to add it the first time you ask for a tracker write.
* **analyst** (PRD Builder) — Captures product and business requirements as a PRD or BRD.
* **data-scientist** (Squad Data Scientist) — Profiles data, builds exploratory-analysis notebooks and dashboards, authors dataops pipelines and feasibility studies, and sets up ML experimentation.
* **prompt-engineer** (Squad Prompt Engineer) — Authors, refactors, and analyses prompt artifacts (prompts, instructions, agents, and skills) and designs AI evaluation datasets.
* **technical-writer** (Squad Technical Writer) — Authors and maintains documentation that stays in step with the code.
* **presenter** (PowerPoint Subagent) — Builds slide decks and executive summaries through the PowerPoint skill pipeline.
* **experimenter** (Experiment Designer) — Designs a Minimum Viable Experiment to validate the riskiest assumption.
* **cost-manager** (Squad Cost Manager) — Estimates Azure cost and applies FinOps and Well-Architected cost guidance.
* **azure-architect** (Squad Azure Architect) — Authors Azure high- and low-level designs with AVM modules and landing-zone patterns.
* **iac-author** (Squad IaC Author) — Converts an Azure design into Bicep or Terraform; authors IaC but never deploys.
* **deployer** (Squad Deployer) — Runs Azure deployments behind a human approval gate; defaults to a read-only dry run.
* **release-engineer** (GitHub Actions Expert) — Builds and hardens the delivery pipeline: GitHub Actions workflows, pinned actions, OIDC instead of stored secrets, CI cost and duration, environments and approvals, and the rollout and rollback plan. Builds the pipeline; never runs a deployment itself. Opt-in: rests on a registered external agent you install.
* **aws-architect** (aws-principal-architect) — Designs AWS workloads: which services, how the accounts and landing zone are laid out, and the CDK, SAM, or CloudFormation to build it, judged against the AWS Well-Architected pillars. Part of the `aws` pack, so it arrives only when that pack is installed and applied.
* **aws-diagnose** (AWS Incident Triage) — Works an AWS incident read-only from the alarm to an evidence-backed root-cause hypothesis, using CloudWatch metrics, logs, and traces; recommends a mitigation but never applies one. Part of the `aws` pack.
* **modernizer** (Squad Modernization Planner) — Plans framework, dependency, and cloud-migration modernization; SQL migration recommendation and prerequisite/readiness cues resolve to Squad SQL Migration Advisor.
* **asbuilt-author** (Squad As-Built Author) — Documents already-deployed infrastructure (inventory, compliance, runbook, DR); strictly read-only.
* **azure-diagnose** (Squad Azure Diagnose) — Triages deployed Azure resources read-only into ranked hypotheses and leads the incident through to its root-cause record; recommends but never applies fixes.
* **pp-architect** (Power Platform Expert) — Designs the Power Platform solution: which of Power Apps, Power Automate, Power Pages, Dataverse, and Copilot Studio to use, how environments and DLP policies are set up, and how the data model and solution lifecycle hang together. Part of the `power-platform` pack, so it arrives only when that pack is installed and applied.
* **pp-connector** (Power Platform MCP Integration Expert) — Builds Power Platform custom connectors and wires them into Copilot Studio agents; writes the connector files and prepares them for certification, but never publishes one to your tenant. Part of the `power-platform` pack.
* **m365-agent-architect** (Declarative Agents Architect) — Designs a Microsoft 365 Copilot declarative agent: what it should be able to do, how its instructions and conversation starters read, and whether it is defined as a JSON manifest or in TypeSpec. Writes the definition and the design notes; never provisions the agent in your tenant. Part of the `m365-copilot` pack, so it arrives only when that pack is installed and applied.
* **m365-agent-integrator** (MCP M365 Agent Expert) — Connects a Microsoft 365 Copilot agent to the systems behind it: MCP server tools, sign-in through Entra, and Microsoft Graph. Plans the tenant rollout and stops at the handoff rather than deploying it for you. Part of the `m365-copilot` pack.
* **intake-validator** (PRD Quality Reviewer) — Validates that requirement and input artifacts are complete and clear before the squad builds on them. Seeded in the `product` and `full` profiles; addable to any roster; dispatched only by the conditional intake gate.
* **scribe** (Squad Scribe) — Writes squad state: decisions, history, and memory. Always included; never optional.
